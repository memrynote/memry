import { notYetImplemented } from './_not-implemented'

notYetImplemented(
  'capability:check',
  'M3 (capability hardening)',
  'validate the Tauri capability JSON against the allow-list of invokable commands'
)
