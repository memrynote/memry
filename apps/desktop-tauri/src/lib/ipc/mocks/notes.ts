import type { MockRouteMap } from './types'

// M5 graduated notes CRUD/folder/property/CRDT calls to real Rust commands.
// Deferred attachment/import/export/version routes live under ./stubs.
export const notesRoutes: MockRouteMap = {}
