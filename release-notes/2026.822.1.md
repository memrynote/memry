## Fixes
- 🪟 Windows Start menu shortcut restored — every install now recreates the Start menu entry if it's missing.
- 🤖 Claude and Codex options always visible — the CLI providers now show up in agent chat even when they aren't detected yet, so you can see how to set them up.
- 📊 Smoother table cell selection — dragging across table cells now selects just those cells instead of grabbing the whole table.
