# Specification defect log

**Feature**: 002-native-foundation-ios | **Gate**: G3 (zero open entries), G5 (re-checked)

## The rule

SC-002 says the protocol specification is complete enough to build against. This file
is how that is measured rather than asserted.

**Every question that required reading TypeScript source during core development is
logged here.** If a Rust author had to open `packages/sync-client`, `apps/desktop` or
`apps/sync-server` to find out what the protocol does, the specification did not say
it, and that is a defect in the specification, not a note about the reader.

**An entry is closed by a change to the specification**, meaning a chapter under
`docs/protocol/` and, when a format is involved, the vectors that pin it. An entry is
not closed by writing the answer into Rust code, into a comment, into a commit
message, or into this file's notes column.

**Zero open entries is a gate.** G3 requires it before Rust feature work proceeds past
the vector tier, and G5 re-checks it before the headless round trip counts as
evidence. An open entry at either point stops the phase; it does not get carried.

Log the entry when the question is asked, not when it is answered. An entry that was
opened and closed in the same afternoon is the healthy case and is still worth having,
because the count of entries per chapter is the only signal available about which
chapters are thin.

## Entries

| #   | Date       | Chapter | Question                                                                                                                                                                                                                                                                                                                                                               | Source that had to be read                                                                                                                                                                                       | Spec change that closed it                                                                                                                                                                                                                                                                                                           | Status     |
| --- | ---------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- |
| 1   | 2026-09-13 | 04 §4.1 | Which deflate implementation and which compression level reproduce `pako.deflate`'s bytes? §4.1 said "a zlib wrapper — `flate2::ZlibEncoder`, not `DeflateEncoder`", which is necessary and not sufficient: `flate2`'s default `miniz_oxide` backend and its `zlib-rs` backend both emit a valid, longer, non-identical stream at level 6 and fail `compression.json`. | `node_modules/pako` run directly against the committed vector to recover the level, and `python3 -c "import zlib"` to confirm stock zlib reproduces it. The chapter named neither a level nor an implementation. | New §4.1.1, "The deflate implementation is part of the format": level 6, stock zlib, the explicit `default-features = false, features = ["zlib"]` selection, the three backends that fail, and why a round-trip test cannot catch any of it. No vector changed — the bytes were always right, the chapter's guidance was incomplete. | **Closed** |

| 2 | 2026-09-13 | data-model §A.0 | The migration convention mandates setting `user_version` **outside** the migration transaction but never says the statements must be idempotent. Taken literally with plain `CREATE TABLE`, a crash between the commit and the pragma is unrecoverable rather than merely retried: the transaction is all-or-nothing, so on the re-run every object already exists and the migration can never make progress again. What makes that ordering safe? | None. The gap surfaced while writing `0001_baseline.sql`; no TypeScript was read. | §A.0 extended with "Every migration statement must be idempotent", naming `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS` and `INSERT OR IGNORE`, and stating why the counter stays outside the transaction anyway. No vector involved. Covered by a migration-runner test for the crash window. | **Closed** |
| 3 | 2026-09-13 | data-model §A.4 | "Every projection table carries three columns not listed per table below: `clock`, `synced_at`, `deleted_at`" is not expressible for `settings_field_clocks`, which is defined as `path TEXT PRIMARY KEY, clock TEXT NOT NULL` — its `clock` _is_ the per-path clock the table exists for, so the rule demands a second column of the same name. Which tables does the rule cover? | None. The gap surfaced while writing `0002_projections.sql`; no TypeScript was read. | §A.4 now scopes the rule to tables that project one sync item to one row, and defines a **side table** exception carrying exactly its listed columns. `settings_field_clocks` is named as the only side table in the section. No vector involved. | **Closed** |
| 4 | 2026-09-13 | data-model §A.2, §A.4 | Index coverage is specified for `sync_items` and `outbox` only. Nothing says whether the projections need indexes (`notes.folder_path`, `tasks.project_id`, `note_tags.tag`, `project_statuses.project_id`), so a baseline author must either invent them or leave a silent gap. | None. The gap surfaced while writing the baseline migrations; no TypeScript was read. | §A.4 now states that no indexes are specified for the projection tables and the baseline creates none, that §A.2's two tables are the only fixed index coverage, and that a projection index is additive, lands in a later migration, and is justified by a measured query. No vector involved. | **Closed** |

| 5 | 2026-09-13 | 04 §4.8 | Where does `signerDeviceId` come from? The `PushItem` table lists it as required, but no chapter says it is the id of the device whose signing key produced `signature` rather than the authoring device or the session's device id. It appears only in the vectors' expected output, never in their input, so a writer cannot derive it from the vector alone. | `packages/contracts/scripts/vectors/record-envelope.ts`, the vector generator. Inside `packages/contracts`, which the brief names as the byte-level contract, so no `sync-client`/`desktop`/`sync-server` source was read — but the chapter, not the generator, should have answered it. | Chapter 04 §4.8 now states normatively that `signerDeviceId` is the id of the device whose Ed25519 signing key produced `signature`, that a conforming client derives it from the same key material it signs with in one step, and why the verifier's key resolution (chapter 01 §1.4) makes the distinction load-bearing. No vector changed — the bytes were always right. | **Closed** |
| 6 | 2026-09-13 | 05 §5.3 | §5.3's resolution table turns on whether a header entry is "recognised" but never defines the recognition set, so a client that declares anything outside the fixed thirteen cannot predict the resolved set. Chapter 13 §13.1's "Twelve more" listed thirteen names, `attachment` among them, which made the arithmetic ambiguous too. | None. Resolved from chapter 13 §13.1 and §13.8 by arithmetic; no implementation source was read. | §5.3 now defines recognised as the twenty-five record types — §13.1's thirteen plus twelve — with `attachment` explicitly not recognised because §13.8 says it never travels as a record, and states that an unrecognised entry is dropped individually and silently. §13.1 reworded so the twelve are twelve record types and `attachment` is named separately as the twenty-sixth `SYNC_ITEM_TYPES` member. No vector changed. | **Closed** |

| 7 | 2026-09-13 | 07 §7.16 | How is the Yjs client id derived? Research R2 says "derived from the device id" and stops. The id travels inside every update and two devices minting the same one corrupt a document that merges both, so it is a protocol fact, not an implementation choice — a second port cannot reproduce it from any chapter. | None. Resolved from `yrs`'s own `ClientID` documentation; no implementation source was read. | New §7.16, "The Yjs client id is derived, not chosen": the first 53 bits of the SHA-256 of the device id, big-endian, with the reason for 53 bits (a JavaScript peer holds it in a `number`) and for hashing the device id rather than randomising per launch. No vector changed. | **Closed** |
| 8 | 2026-09-13 | data-model §A.2 | §A.2 gives the local sequence as `MAX(seq) + 1` read inside the writing transaction, and separately says a fold deletes the rows it absorbed. Taken together the counter walks backwards and re-issues a used sequence the first time a compaction lands. Which is it? | None. The contradiction is internal to §A.2; no implementation source was read. | §A.2's namespace table now says "one past the greatest sequence this namespace has ever issued", with a new paragraph requiring the `MAX` be taken over `yjs_updates.seq` **union** `yjs_snapshots.last_seq` — the same union §A.2 already required for reads, applied to the write side where it had been left implicit. Covered by a regression test that folds and then appends. | **Closed** |
| 9 | 2026-09-13 | data-model §C.4 | §C.4 annotates `Unreadable` as "terminal for this document until the app restarts" and also draws an `Unreadable -> Closed` edge. A plain reading of the diagram sends the next request from `Closed` to `Loading`, which contradicts the annotation. Which wins? | None. The contradiction is internal to §C.4; no implementation source was read. | §C.4 now separates the two scopes: the edge is about residency (an unheld document is evicted like any other), the annotation is about the verdict (which survives eviction, in memory, until relaunch). A later request still walks `Closed -> Loading -> Unreadable` but reaches it from the remembered verdict without replaying a log that already failed. | **Closed** |
| 10 | 2026-09-13 | 13 §13.10.1 | Which settings groups are modelled? §13.10 turns entirely on "a group this client does not model", and `payload-schemas.json` requires `general` kept and `experimental` stripped, so the list is load-bearing — but no chapter enumerates it. | **`packages/contracts/src/settings-sync.ts:5-118`.** This is the only TypeScript read during the whole wave, and it is contracts source rather than the `test-vectors/` directory the README designates as the byte-level contract. | New §13.10.1 enumerating the ten modelled groups — `general`, `editor`, `tasks`, `calendar`, `keyboard`, `notes`, `sync`, `inbox`, `journal`, `sidebar` — and stating that the modelled/unmodelled split is part of the format because the vectors pin it. Modelling fewer is conforming; modelling more is not. | **Closed** |
| 11 | 2026-09-13 | 13 §13.7.10 | §13.7.10 called `icon` "the only non-optional field on any subscribed type", which contradicts §13.7.7 to §13.7.9 marking `name`, `color`, `sortOrder` and `type` required on their own types. Which is authoritative? | None; resolved within chapter 13. No implementation source was read. | §13.7.10 no longer makes a cross-type claim. It says `icon` is nullable rather than optional (the key is always present, the value may be `null`) and states the precedence rule: where a per-type section marks a field required, that section wins. | **Closed** |
| 12 | 2026-09-13 | 13 §13.7.11 | `custom_icon` is one of the thirteen subscribed types but §A.4 lists no projection table and the baseline creates none. Is that an omission or intentional? | None; resolved from §13.7.11's existing self-healing rule. No implementation source was read. | §13.7.11 now states normatively that `custom_icon` is a reader-only type with no projection table, why (one inline blob, no field a query filters or sorts on), and that a client may add one but it must never be the source. Implemented as a reader with a no-op writer; no migration added. | **Closed** |
| 13 | 2026-09-13 | data-model §A.4 | §A.4 declares NOT NULL columns for fields chapter 13 §13.7 marks optional (`notes.title`, `tasks.project_id`, `projects.color`, `reminders.remind_at`, `templates.name`). No chapter says what a projector does when the key is absent — substitute, or refuse the item? | None; resolved from §13.3's forward-tolerance rule. No implementation source was read. | §A.4 gained "A NOT NULL projection column does not mean a required payload field": the projector substitutes the column default and never refuses, because §13.3 requires an older client to keep applying what a newer one writes. The NOT NULL exists so queries never handle a null, not to assert a wire guarantee. | **Closed** |
| 14 | 2026-09-13 | data-model §A.4, 13 §13.7.7 | `tag_definitions.color_authored` is `INTEGER NOT NULL DEFAULT 0`, two states, but §13.7.7 gives the payload three: true, false, and absent meaning "cannot tell, honour the colour". Absent is behaviourally true and behaviourally not false, so the column cannot hold what the payload says. | None; the mismatch is between §A.4 and §13.7.7. No implementation source was read. | §A.4 now states the projection maps absent to 1 on purpose, collapsing absent and true because the column answers only "may the palette overwrite this colour". The distinction is not lost from the vault — it survives verbatim in `sync_items.payload`, the source of record — only from the rebuildable projection. | **Closed** |
| 15 | 2026-09-13 | 13 §13.7.2 | §13.7.2 defines `journal` by reference as "same shape as `note` minus the file fields, plus `date`", which leaves `title`, `emoji`, `aliases` and `pinnedTags` ambiguous. No vector exercises them either way, so two ports could diverge silently. | None; resolved by reading §13.7.1 alongside it. No implementation source was read. | §13.7.2 now spells the list out: `journal` carries `title`, `emoji`, `aliases`, `properties`, `tags`, `pinnedTags` and `date`, and does not carry `fileType`, `mimeType`, `attachmentId`, `attachmentReferences` or `folderPath`. A clarification of intent, not a change — no vector moved. | **Closed** |
| 16 | 2026-09-13 | data-model §A.4 | `note_tags.pinned_at` is an instant column but `pinnedTags` in the payload carries membership, not a time. Where does the value come from, and is it synced? | None. No implementation source was read. | §A.4 now says `pinned_at` has no source instant: the projector records the apply time, the column is a local ordering hint only, never synced and never compared across devices, and two devices holding different values for the same pinned tag is correct rather than drift. | **Closed** |
| 17 | 2026-09-13 | plan.md Technical Context | plan.md pins Rust `1.89` and `yrs 0.27.4` in the same table. They are mutually impossible: yrs 0.27.4 uses an `if let` match guard, unconditional and not feature-gated, which is unstable before 1.95. No Rust past the CRDT tier could be built until one pin moved. | None. Measured directly by installing each toolchain and building: 1.89, 1.91, 1.93 and 1.94 fail with E0658; 1.95 and 1.98.1 compile. | plan.md's Technical Context, `crates/rust-toolchain.toml`, the workspace `rust-version` and both CI jobs raised to 1.98.1 (Kaan's choice over the measured 1.95 floor, for headroom), in commit `b62a6a9a3`. All three iOS targets installed on the new toolchain. The one clippy finding the jump surfaced was fixed rather than allowed. | **Closed** |

| 18 | 2026-09-13 | 04 §4.11.1 | `crdt-update.json`'s four error cases were pinned only by `expectErrorContains`, an English message string from the TypeScript writer ("CRDT update too short", "Signature verification failed"). A Rust or Swift port cannot assert that without copying one implementation's phrasing instead of the protocol's behaviour, and nothing in any chapter named the rejection conditions as distinguishable outcomes. | None; the gap is in the vector's own shape. No implementation source was read. | New §4.11.1 naming exactly two rejection codes, `too-short` and `signature-invalid`, and stating that the wrong note id and a tampered ciphertext are both the latter rather than an AEAD failure. **The vectors changed with the chapter**: `expectErrorCode` added to the generator, `crdt-update.json` regenerated, and the TypeScript verifier now fails if a message-asserting case lacks a code. The Rust port asserts the code and deliberately does not assert the message. | **Closed** |

| 19 | 2026-09-13 | 02 §2.1.1 | §2.1's route table names each schema as a `packages/contracts/src/auth-api.ts:<line>` reference and spells out fields only for `DeviceRegisterRequestSchema` (§2.3). The **response** shapes for OTP request/verify, device register, refresh and setup-token renewal, and the request shapes for refresh and renewal, appear in no chapter. A Rust author cannot deserialise a single `/auth` response from the specification. | **`packages/contracts/src/auth-api.ts`, lines 1 to 120.** Contracts source, not the `test-vectors/` directory the README designates as the byte-level contract. | New §2.1.1 writing out every request and response shape this feature uses, with optional marked as absent-never-null, plus the two facts a reader would otherwise guess: the verify response's token fields are optional on the same response because which arrive depends on account state, and the refresh response is the one auth response with no optional fields because §2.9's rotation would otherwise strand the client. No vector involved. | **Closed** |
| 20 | 2026-09-13 | 02 §2.5.1 | §2.5 defines renewal as "proof of possession of the committed device key" and `RenewSetupTokenRequestSchema` carries `challengeNonce` and `challengeSignature`, but no chapter says **what bytes are signed**. §2.3.1 spells the construction only for `POST /auth/devices`. | **`apps/sync-server/src/routes/auth.ts:508-556`.** Server source. The chapter should have carried the construction. | New §2.5.1: renewal reuses §2.3.1's construction exactly — Ed25519 detached over UTF-8 of `` `${challengeNonce}:${jti}` `` where `jti` is the presented setup token's, standard base64, no prefix, no CBOR. Also records the two consequences the request shape does not imply: the renewed token carries a **new** `jti` that registration must re-read, and presenting a grant retires it into §2.3.3's single-use ledger, so renewal replaces rather than extends. | **Closed** |
| 21 | 2026-09-13 | 00 §0.6.1 | Chapters 03 §3.9, 05 §5.6 and 07 §7.10 each set `maxRetries`, `baseDelayMs`, `retryOn429` and `retryOn5xx` on a retry helper that **no chapter defines**. Which statuses retry by default, the delay formula, whether jitter applies, whether `retry-after` overrides or adds, and which transport errors count were all unspecified — three chapters configuring a thing that does not exist. | None. Derived from §0.5's retryable row, §0.6's `retry-after` rule, §2.10's `base * 2^attempt` shape and the `TransportError` doc comments already in the crate. No implementation source was read. | New §0.6.1 defining the ladder: `baseDelayMs * 2^attempt`, **no jitter** (one device per account, so there is no herd, and a deterministic ladder is testable on a paused clock), `retry-after` replaces rather than adds, a default-retry table where only 500/502/503 and 429 retry and `501` explicitly does not, and transport failures retry except TLS and cancellation. States that a `401` is not part of the ladder in either direction. | **Closed** |
| 22 | 2026-09-13 | 02 §2.10.1 | §2.10 specifies the proactive refresh schedule and the rejection latch, and motivates the latch with "roughly fifteen demand-driven callers" — which implies a reactive refresh path exists but never defines it. What an ordinary authenticated request does on a `401` was unspecified. | None. No implementation source was read. | New §2.10.1: exactly one refresh and one replay, through the same single-flight so fifteen concurrent 401s cost one refresh; the replay costs no backoff and consumes no retry attempt because the failure was a stale credential rather than a transient fault; a second `401` fails the request outright; a failing refresh falls to §2.10's latch. | **Closed** |

| 23 | 2026-09-13 | 12 §12.1.3 | §12.1 described `extract_text` in one sentence — "keeps headings and list markers, drops everything else" — while §12.11 makes it the basis of the SC-010 cross-shell digest. Every byte-level rule (heading level clamped to 1..6, `1. ` for **every** ordered item, one line per table cell, `divider` dropped, callout type and toggle state dropped, unknown elements walked rather than dropped, an empty line block dropped after trimming) existed only in `text-extract.json` and the reference port. A port written from the chapter alone cannot reproduce the bytes. | `packages/contracts/scripts/extract-text.ts`, the generator named in `meta.extractor`. Inside `packages/contracts` rather than one of the four restricted trees, but the chapter should have carried the rules. | New §12.1.3, "What `extract_text` does, rule by rule": a node-class table (containers, line blocks, list items, skipped, and everything-else-is-walked), plus the six rules that are easy to get wrong, each tied to the vector case that pins it. §12.11 reworded to point at §12.1.3 for the rules and the vectors for the bytes. No vector changed. | **Closed** |
| 24 | 2026-09-13 | 12 §12.1.3.1 | The reference extractor renders an `XmlText` with its formatting marks as XML tags and then strips them with `/<[^>]*>/g`, which also removes text the user typed: a note containing `a <b> c` extracts as `a  c`. The loss reaches the FTS index, the preview and the SC-010 digest, silently, and no chapter mentions it. | `packages/contracts/scripts/extract-text.ts`. | New §12.1.3.1, marked **not normative — a defect, recorded so it is not rediscovered as a surprise**. States that a conforming port reproduces it, because parity is what the digest measures and a port that fixed it would report a false mismatch against every other shell, and that fixing it is a coordinated change across every port and its vectors. §12.11 now also warns that a digest match does not prove the text survived — only that both shells lost the same bytes. **This is a real content-loss path in the shipped product and wants a tracking issue; it is not one of #2179 to #2187.** | **Closed** |
| 25 | 2026-09-13 | 12 §12.1.3 | An empty list item and an empty paragraph behave differently and nothing said so: marker-plus-empty-text survives the end-trim as a bare `- `, while an empty paragraph is dropped. The first implementation guessed the other way. | None. Found by a failing test against the committed vector; no implementation source was needed for this one. | §12.1.3 states both halves of the rule and why — the line is trimmed at its end and dropped if nothing remains, and a marker is already non-empty. No vector changed. | **Closed** |
| 26 | 2026-09-13 | 12 §12.1.3 | The end-trim is JavaScript's `trimEnd`, which also trims `U+FEFF`. Rust's `trim_end` does not. Nothing said which trim the format means, so a note beginning with a byte-order mark would digest differently on two shells. | None. Found from the `trimEnd` semantics themselves; no implementation source was read. | §12.1.3 names `trimEnd` explicitly, including `U+FEFF`, and says a port must add it rather than assume its standard library agrees. No vector changed. | **Closed** |

| 27 | 2026-09-13 | 05 §5.11.1 | The **response shapes of the record routes are never given.** §5.12 names `{items, deleted, hasMore, nextCursor}` and speaks of "a ref row", but no chapter says what a ref row contains, what `/sync/pull` answers, or whether `nextCursor` is a string or a number. A port must guess and find out on staging. | `packages/contracts/src/sync-api.ts` (the `SyncItemRef`, `ChangesResponse`, `SyncManifest` and `PushResponse` shapes) and `apps/sync-server/src/routes/sync.ts`. The subagent guessed tolerantly; the orchestrator read the source to write the chapter correctly rather than enshrine the guess. | New §5.11.1 giving the ref row's six fields, and the three response objects. Records two facts the guess got wrong or loose: `nextCursor` is an **integer, not string-or-number**, and it is required on `/sync/changes` but optional on `/sync/manifest`. Also states that a ref row carries no ciphertext and that `rejected` on push is per item, so a partially accepted batch is normal. | **Closed** |
| 28 | 2026-09-13 | 05 §5.12.2 | §5.12.1 establishes that a `deleted[]` entry is an id with **no type**, while all other client bookkeeping is keyed `(type, id)`. No chapter reconciles them, so "where does this row go" was unanswered. | None; the contradiction is between two sections of chapter 05. No implementation source was read for this one. | New §5.12.2: a bare tombstone is recorded **keyed by id alone**, in a set consulted by id **before** any apply, and explicitly not stored as a row of a guessed type. Spells out why the ordering is the point — an item arriving on a later page must not resurrect, including for an id this client has never seen. | **Closed** |
| 29 | 2026-09-13 | 11 §11.7.1 | Data-model §C.3 draws an `Unentitled` state with no stated source. `clientPolicy` on `GET /sync/status` (§11.8) carries only `platform`, `writesEnabled` and `minWriteVersion` — no entitlement field — so a client polling it for entitlement waits forever. | None. Resolved from chapter 00 §0.5's error table, which carries `SYNC_PAYMENT_REQUIRED` at 402. No implementation source was needed, though the server confirms it. | New §11.7.1 tabulating all three write-blocked states against their sources, and stating that `Unentitled` is entered **reactively on a 402 `SYNC_PAYMENT_REQUIRED`**, never predicted. Consistent with §11.3, a client starts entitled and is demoted, because starting unentitled locks a paying user out whenever the first status call fails. **Implementation gap: the engine currently takes entitlement as an explicit input defaulting to entitled — correct default, not yet wired to the 402. Carried into W6 with T118.** | **Closed** |
| 30 | 2026-09-13 | 01 §1.4.0 | Chapter 04 §4.8 has the verifier "resolve a public key by `signerDeviceId`", presuming a directory no chapter names. There is no endpoint, table or cache described anywhere, so a port cannot verify a record written by another device. | `apps/sync-server/src/routes/auth.ts` (the `GET /auth/devices` handler). Read by the orchestrator to write the chapter; the subagent avoided the question by making `RecordCipher` a one-method seam rather than inventing a directory. | New §1.4.0: the directory is `GET /auth/devices`, whose `devices[]` carry `signingPublicKey` — the Ed25519 key committed at registration (§2.3). Also states the consequence nobody had written down: an unresolvable `signerDeviceId` makes the record **unverified, not invalid**, recorded as unapplied rather than dropped, because a device registered since the last fetch is the ordinary case and not an attack. | **Closed** |
| 31 | 2026-09-13 | 06 §6.4.2 | §6.4.2's three canonicalisation rules do not say what `-0` becomes. IEEE 754 has two spellings of zero; `-0 == 0` in every language involved, so a naive comparison calls them equal while a naive serialiser writes `-0` and makes the canonical forms differ — a silent divergence between two conforming-looking ports. | None. No implementation source was read. | §6.4.2 now states that negative zero canonicalises to `0`, names why the hole is easy to fall into, and notes this matches `JSON.stringify(-0)`. No vector changed — no committed case exercises `-0`, which is itself why it went unnoticed. | **Closed** |

| 32 | 2026-09-13 | 08 §8.6.1 | `pack-container.json`'s five error cases were pinned only by `expectErrorContains`, and the over-ceiling case pinned `pack truncated` — **a message a conforming reader must not produce**. §8.6 itself says `parsePack` does not enforce `PACK_MAX_ENTRIES` and that a conforming reader MUST apply the cap before allocating, which is an earlier and different failure. Reproducing the message would mean deleting the check the chapter mandates. | None; the contradiction is between §8.6 and the vector. The subagent reported it from the chapter alone. | New §8.6.1 naming the five codes, following chapter 04 §4.11.1's precedent. **The vectors changed with the chapter**: `expectErrorCode` added to all five, the over-ceiling case's message demoted to `referenceOnlyMessage` with no `expectErrorContains`, `pack-container.json` regenerated, and the TypeScript verifier now requires a code on every case and tolerates a missing message. The Rust port dispatches on the code. | **Closed** |
| 33 | 2026-09-13 | 08 §8.3 | §8.3 states that a negative `sortKey` decodes as `0` but not **how**, and the stated outcome is not reproducible from a naive reading: a signed decode gives `-1`, and so does `hi_i32 * 2^32 + lo_u32`. Two readers that both follow the chapter can therefore disagree about entry order. | `packages/contracts/src/pack-format.ts`, the reader `meta.reader` names. Permitted, and reported, because the chapter should have carried the mechanism. | §8.3 now gives the mechanism: two **unsigned** halves into a double as `u32 * 2^32 + u32`, where doubles are spaced 2048 apart, so `0xFFFF_FFFF_FFFF_FFFF` rounds up to exactly `2^64` and the `v - 2^64` branch yields `0`. No vector changed — the committed bytes were already right. | **Closed** |
| 34 | 2026-09-13 | 08 §8.3 | Nothing says what a reader does with invalid UTF-8 in `idBytes` or `keyBytes`. The chapter says the fields are UTF-8 and stops, so a port may reasonably reject the pack while the reference substitutes U+FFFD. | `packages/contracts/src/pack-format.ts`. | §8.3 now states that invalid UTF-8 is **replaced, not rejected**, with the reason: a corrupt byte makes one entry unaddressable, while rejecting makes the whole pack unreadable, and §8.10 already says coverage is not total. Recorded as uncovered by any committed case. | **Closed** |

| 35 | 2026-09-13 | quickstart §Preconditions | **No document states what `--server staging` resolves to.** Not the quickstart, not plan.md, not any chapter. The product reads `SYNC_SERVER_URL` from a gitignored `.env.<environment>`, so the value exists nowhere a reader can find it, and a worktree missing that file silently falls back to `http://localhost:8787`. | `apps/desktop/scripts/check-cert-hashes-config.test.ts` and `apps/desktop/src/main/calendar/google/push-runtime.ts` by the subagent; the orchestrator then read `packages/sync-client/src/sync-server-url.ts` and the linked `apps/desktop/.env.staging` to get the authoritative value rather than enshrine a guess from a test fixture. | Quickstart preconditions now name all three — `https://sync-staging.memrynote.com`, `https://sync.memrynote.com`, `http://localhost:8787` — say they are not hardcoded anywhere in the product, name the resolver, and say why `pnpm env:check` is not optional: a worktree without the gitignored env file falls back to localhost silently. | **Closed** |
| 36 | 2026-09-13 | 05 §5.11.1 | §5.11.1 deliberately wrote down the record routes' **response** shapes and left the `POST /sync/push` **request** unstated, named only as a schema symbol. A port has to guess the envelope. | None by the subagent, which guessed `{items: [...]}` by symmetry and said so; the orchestrator confirmed against `packages/contracts/src/sync-api.ts`. | §5.11.1 now gives the request as `{ items: <push item>[] }` with the **1 to 100** bound, and says the push item is §4.8's shape minus `stateVector`. The guess was right; the bound was not stated anywhere. | **Closed** |
| 37 | 2026-09-13 | 07 §7.4 | `POST /sync/crdt/updates` is the route a client uses to push a body, and no chapter said what to send. §7.11 gives read shapes and §7.4 the response, but not the request. | None by the subagent, which guessed and said so; the orchestrator confirmed against `apps/sync-server/src/routes/sync.ts:640`. | §7.4 now gives `{ noteId, updates: string[] }`, at most 100 per call, each base64 string capped at **twice** `MAX_UPDATE_BYTES` — with the reason, that base64 inflates by four thirds and the cap applies to the encoded form. The guess was right; the doubling was not stated. | **Closed** |
| 38 | 2026-09-13 | 05 §5.11.1 | `GET /sync/vaults` had no response shape, though chapters 01 §1.7 and 03 §3.10 both depend on it. | `apps/sync-server/src/routes/sync.ts` by the orchestrator. | §5.11.1 now gives `{ vaults: [...] }` and states the two facts a reader gets wrong: it is an object with a `vaults` key rather than a bare array, and the route is account-scoped, above the vault middleware, so it takes no `X-Memry-Vault-Id`. | **Closed** |
| 39 | 2026-09-13 | 02 §2.1.1 | §2.1.1 wrote out every auth shape except `GET /auth/devices`, and chapter 01 §1.4.0 referred to "the device id" without naming the field — so a port must guess between `id` and `deviceId`. | `apps/sync-server/src/routes/auth.ts` by the orchestrator. | §2.1.1 now gives `{ devices: [{ id, name, platform, signingPublicKey, revokedAt }] }` and states the field is spelled `id`. §1.4.0 points at it. | **Closed** |
| 40 | 2026-09-13 | 01 §1.4.0 | §1.4.0 says an unresolvable signer is "unverified, not invalid" but gives the outcome no name, so the nearest existing code is `signature-invalid` — which means tampering. | None. | §1.4.0 now states it is **its own outcome** and that a client MUST NOT report it as `signature-invalid`, with the consequence spelled out: that code would turn "we have not refetched the device list yet" into a security incident in the logs and tell a user their data is corrupt when it is merely new. | **Closed** |
| 41 | 2026-09-13 | 11 §11.7.1 | §11.7.1 defined how a client **enters** `Unentitled` and not how it leaves. `clientPolicy` carries no entitlement field and a parked outbox attempts no write that could earn a 2xx, so the state was terminal as written, and data-model §C.3's `Unentitled -> Idle` edge annotated "the policy cleared on the next status poll" was unreachable. | None. | §11.7.1 now states that leaving is **not a sync-tier event**: the account tier clears it after a purchase or billing refresh, and a client MUST NOT invent a probe write. §C.3's edge annotation corrected to match, and the three states' differing clear paths tabulated. | **Closed** |
| 42 | 2026-09-13 | data-model §C.3 | §C.3 drew no `Pushing -> Unentitled` edge although §11.7.1 makes a mid-wave `402` reachable on `/sync/push`. Without it a billing fact is reported as `Failed`, which accrues backoff against a condition the user cannot retry away. | None. | §C.3 gained the edge. Also recorded in chapter 11: only `SYNC_PAYMENT_REQUIRED` demotes — `SYNC_VAULT_LIMIT_EXCEEDED` is also a `402` and means the plan is active and a limit inside it was hit. | **Closed** |
| 43 | 2026-09-13 | data-model §A.2 | `outbox.last_error` was specified as "truncated" with no length, so two clients would store different values for the same failure. | None. | §A.2 now caps it at **500 characters**, with the reason: a server error body must not turn a queue row into a log sink, and "truncated" without a number is not reproducible. | **Closed** |
| 44 | 2026-09-13 | data-model §A.2 | §A.2 says a row may be "acknowledged locally with a recorded reason", but the row is deleted and no table holds the reason — so the mechanism did not exist. | None. | §A.2 now states it is one transaction and not an archive: `last_error` is written and the row deleted together, and the reason is reported in the wave's result. Explicitly: there is no dead-letter table and this document defines none, because a queue that keeps undeliverable rows never drains. | **Closed** |
| 45 | 2026-09-13 | tasks.md T113 | T113 named six commands ending in `notes show`; `quickstart.md` §G4 invokes `notes text <id>` and `notes state-vector <id>`. The two artefacts disagreed about the command surface a gate checks. | None. | The quickstart wins — it is what G4's evidence is recorded against — and T113's text is reconciled to it. | **Closed** |

| 46 | 2026-09-13 | 07 §7.4.1 | `POST /sync/crdt/updates/batch` has **no request shape anywhere in chapter 07**. §7.2 names the route, §7.3 caps it at 100 documents with duplicates rejected, §7.3.1 says a new client SHOULD prefer it for a sweep, and §7.11 gives its response — but the request body's field names appear nowhere, so the route §7.3.1 recommends is the one a port cannot call. | `apps/sync-server/src/routes/sync.ts` by the orchestrator. The subagent avoided the guess entirely by driving the single-document `GET` instead, whose request §7.2 does spell out. | New §7.4.1 giving all four CRDT bodies. The batch request is `{ notes: [{ noteId, since }], limit }`, at most 100, duplicates **rejected outright** rather than de-duplicated, `since` default 0, `limit` 1 to 100 default 100. | **Closed** |
| 47 | 2026-09-13 | 07 §7.4.1 | The single-document `GET /sync/crdt/updates` **response** was given only by analogy: §7.11 writes the batch form's `{ notes: { <id>: { updates, hasMore } } }` and never the single form's envelope. The implementation had to accept both shapes to be safe. | `apps/sync-server/src/routes/sync.ts` by the orchestrator. | §7.4.1 states the single form is `{ updates, hasMore }` **at the top level**, with no `notes` wrapper, and says so explicitly _because_ the batch form is keyed by id and the two are easy to conflate. The tolerant dual-shape reader can now be narrowed. | **Closed** |
| 48 | 2026-09-13 | 07 §7.4.1 | `POST /sync/crdt/snapshot` has no request shape. §7.2 names it and §7.13.4 gives the response; the field name had to be inferred from the batch schema. A wrong field name here is a 400 at the first staging run. | None by the subagent, which inferred `{ noteId, snapshot }` by analogy and said so; the orchestrator confirmed against `apps/sync-server/src/routes/sync.ts`. | §7.4.1 gives `{ noteId, snapshot }` and the batch form `{ snapshots: [...] }` capped at **50** — with the reason, that a snapshot may be 5 MiB decoded and ~6.7 MiB base64 — and notes that `snapshot` is unbounded per entry because an oversized payload is a per-note rejection rather than a malformed batch. The inference was right. | **Closed** |

| 49 | 2026-09-13 | 04 §4.1.1 | Spec defect 1 settled that the format requires **stock zlib**, and stopped there. It never said what that obliges a shell to do: `flate2`'s `zlib` feature binds to the _system_ zlib, a host build links `-lz` automatically, and an app embedding the core as a static library does not — so `libmemry_core.a` arrives with `_deflate`, `_deflateEnd`, `_deflateInit2_` and friends undefined. The iOS app failed to link, and nothing in any chapter or task would have predicted it. | None. Found by running `xcodebuild` and reading the linker output. | §4.1.1 extended with "Choosing stock zlib is a linking obligation on every shell": why the consequence travels, that Apple ships `libz` in every SDK so a Swift shell links it, that the Memry Swift package now names it beside the binary target, and that a platform without a system zlib must supply **stock** zlib rather than any deflate, because the vectors pin its output. Fixed in `packages/swift/MemryCore/Package.swift` with `linkerSettings: [.linkedLibrary("z")]`. | **Closed** |

| 50 | 2026-09-13 | 05 §5.11.1 | `POST /sync/pull`'s **request** body was never written down. §5.11.1 gave the response and stopped. The implementation guessed `{ ids }` and staging answered `400 Invalid pull request: Invalid input: expected array, received undefined`. | None — **found by running the real round trip**, not by reading. The field name was then confirmed in `packages/contracts/src/sync-api.ts`. | §5.11.1 now gives `{ itemIds: string[] }`, 1 to 100, and says explicitly that the field is `itemIds` and not `ids`. The Rust request and the first-sync test were corrected to match. | **Closed** |
| 51 | 2026-09-13 | 05 §5.11.1 | **The read envelope is not the write envelope, and no chapter said so.** Chapter 04 §4.8 describes the push item with the four ciphertext fields flat; `POST /sync/pull` returns them nested under `blob`. §5.11.1 as first written asserted the pull answers `{ items: [<§4.8 envelope>] }` — which was the orchestrator's own guess, written into the chapter, and wrong. | None — found by running the round trip: **500 of 500 items came back `missing string field encryptedKey`**. Confirmed against `packages/contracts/src/sync-api.ts` (`PullItemResponseSchema`, `EncryptedItemPayloadSchema`). | §5.11.1 now carries a side-by-side table of the two shapes and names this the costliest omission in the chapter, because a port that assumes one envelope reads every item as malformed. `envelope::from_json` accepts both spellings, flat first, since the vectors are written from the writer's side. | **Closed** |
| 52 | 2026-09-13 | 05 §5.11.1 | `GET /sync/vaults`'s element shape was unstated — defect 38 added the `{ vaults: [...] }` envelope and not the row. The implementation guessed `id` or `vaultId`; the server sends **`vaultUuid`**, so the CLI reported "this account has no vaults" against an account holding four. | None — found by running the round trip, then confirmed in `apps/sync-server/src/services/sync.ts` (`listUserVaults`, `UserVaultSummary`). | §5.11.1 now gives `{ vaultUuid, itemCount, createdAt, encryptedName, nameNonce }`, names `vaultUuid` explicitly against the two wrong guesses, notes the three nullable fields, and requires a reader that cannot find the id to report a **malformed response rather than skip the row** — skipping is what turned a field-name mismatch into a plausible-looking empty account. The Rust reader was changed from `filter_map` to a hard failure. | **Closed** |
| 53 | 2026-09-13 | 13 §13.7.7 | `tag_definition.icon` was projected as optional-but-not-nullable, so a payload carrying `icon: null` was recorded **corrupt**. 146 of the account's tag definitions do exactly that. | None — found by running the round trip. No committed vector case carries a null `icon`, which is why 52 payload cases and eleven green vector classes missed it. | The projector declares `icon` null-tolerant, which is what §13.3's forward tolerance and data-model §A.4 already required: a projector substitutes the column default and never refuses the item. Recorded as the class of bug only real data finds — `Field::opt` versus `Field::opt_null` is invisible until a producer writes an explicit null. | **Closed** |

| 54 | 2026-09-13 | quickstart §G4, plan.md | **The quickstart named a staging D1 that does not exist.** Four `wrangler d1 execute` invocations in `quickstart.md` and one in `plan.md` addressed `memry-staging`. Every one of them fails, and `wrangler` answers an unknown database with a generic lookup failure that reads like an auth problem rather than a typo — so the gate on the gate is itself a dead end. | `apps/sync-server/wrangler.toml` by the orchestrator, to get the authoritative name rather than guess a second time. | All five invocations corrected to **`memry-sync-staging`**, and §G4's preconditions now state the name in prose with the reason it is not derivable: the three databases are `memry-sync`, `memry-sync-staging` and `memry-sync-production`, so "`memry-` plus the environment" is wrong for two of the three. | **Closed** |

| 55 | 2026-09-13 | 13 §13.7.9 | **`property_definition.type` is marked required and never enumerated.** The vectors carry `select` and `text` only. A port implementing FR-048 ("a property value is never retyped by an edit") has to decide whether `type` constrains the JSON type of a value, and nothing said. | `packages/contracts/src/sync-payloads.ts:322-330` and `property-types.ts:1-50,130-145` by the orchestrator. The subagent avoided needing the answer and explicitly did not open either file. | §13.7.9 now states that `type` is an **open string on the wire and a port MUST NOT close it** — `z.string()`, not an enum, deliberately, for the same forward-tolerance reason `options` is opaque. The ten values desktop writes are tabulated as **informative**, with `relation` marked never-persisted and `project` marked reserved. And the rule the task actually needed: **a port MUST NOT map a type name to a JSON type to validate a value** — no such mapping exists anywhere on the wire, and inventing one refuses a legitimate edit on real data. FR-048 is checked against the value already on the note. | **Closed** |
| 56 | 2026-09-13 | 06 §6.9 | Every clocked settings path in §6.9 is a leaf. Two questions a writer hits immediately were unanswered: may a write clock an **ancestor** of the path it wrote, and does a **removal** tick a clock at all? | None. | New §6.9.1. A write MUST clock the path it wrote and MUST NOT clock an ancestor — ticking `journal` for a change to `journal.weekdayTemplates.3` makes a Wednesday edit beat a concurrent Thursday edit, defeating the per-day clock. **A removal MUST tick**, with the failure spelled out: a removal that ticks nothing loses to the peer still holding the old value and the cleared setting resurrects on the next pull. Pruning descendant clocks under a replaced subtree is explicitly **not** defined and MUST NOT be invented, by §6.10's reasoning. | **Closed** |
| 57 | 2026-09-13 | 13 §13.7.10, data-model §A.4 | **A `folder_config` id is a path, and no chapter said what a rename, a move or a delete does to the subtree.** FR-051 requires the hierarchy to match desktop's. Unanswered: do descendant `folder_config` ids and `note.folderPath` values get rewritten, and does a delete cascade into the notes inside? | `apps/desktop/src/main/vault/notes-crud.ts:885-908` by the orchestrator. The subagent resolved it from risk asymmetry without leaving `docs/protocol/` and flagged it. | New §13.7.10.1. The answer is that on desktop **a folder is a directory**: `renameFolder` is one `fs.rename` and `deleteFolder` is `rm -rf`, so the subtree follows implicitly because `folderPath` is where the file sits rather than a stored field. A core with no filesystem must do it explicitly — rename and move are a **re-key of the whole subtree** from the old row's stored bytes, one transaction, an outbox row per row. **A divergence is recorded rather than hidden**: desktop's delete cascades and `memry-core` refuses a folder still holding a live note, because the prefix rewrite is reversible and `rm -rf` is not and the only caller today is a headless CLI on a real account. Named as an open product question for the first shell with a folder-delete UI, explicitly not settled. | **Closed** |
| 58 | 2026-09-13 | 13 §13.7.6 | §13.7.6 required `template.properties` to stay an array and named neither the **element shape** nor how it maps onto §13.7.1's free-form `properties` record on the note being created. A port cannot apply a template without both. | `packages/contracts/src/templates-api.ts:102-117` and `apps/desktop/src/main/vault/{templates.ts:344-357,notes-crud.ts:271-296}` by the orchestrator. The subagent, lacking the mapping, left template properties unapplied and said so. | §13.7.6 now gives the element as `{ name, type, value: z.unknown(), options?: string[] }` and the mapping as **name and value only** — `properties[prop.name] = prop.value` — with `type` and `options` discarded at note-creation time, and the caller's own properties overlaid on top so the caller wins. Also records that the template `type` enum and §13.7.9's vocabulary have **drifted** (`rating` on one side, `status` and `relation` on the other) and that neither is a validator for the other. | **Closed** |
| 59 | 2026-09-13 | 13 §13.7.2 | §13.7.2 says a journal carries `title` "exactly as `note` does" and never says what a **newly created day's** title is. §13.4 makes absent and `null` different writes, so the two readings are not equivalent. | None. | §13.7.2 now states the key is **absent, not `null`**, and gives the consequence that makes it matter: seeding `title: null` is an explicit clear, so the first peer to merge it wipes a title another device had set. A day's display name is derived from its `date` until someone titles it, and a derived name is not a payload field. | **Closed** |
| 60 | 2026-09-13 | 07 §7.1 | §7.1 settles the **journal** id and gives the grammar, and no chapter says how a plain **note** id is minted. Two ports would have invented two schemes, in the same vault. | `apps/desktop/src/main/lib/id.ts:1-20` by the orchestrator. The subagent took the id from its caller and validated it rather than guessing a scheme. | New §7.1.0. Desktop mints **12 characters from `0123456789a-z`**, lowercase, via `nanoid`'s `customAlphabet` — which is exactly the shape of `dzxnhc9p3gk3`, the id observed on the real account — while tasks and projects use plain 21-character `nanoid`. Normative and deliberately narrower: `NoteIdSchema` is the only wire constraint and a client MUST accept any id meeting it, because journal ids do not match the 12-character form; a minting client SHOULD match desktop; and **a core handed an id MUST validate rather than mint a replacement**, since replacing a caller's id is how one note becomes two. | **Closed** |

| 61 | 2026-09-13 | data-model §A.5, FR-053 | **How do note results and task results combine into one ranked list?** §A.5 pins the two bm25 weight vectors and never says whether the two result sets merge. A port has to decide, and an interleaving invented in one shell is not parity with the other. | None. | §A.5 now states the two lists are ranked **separately and MUST NOT be merged**, with the reason: a bm25 score is relative to the table it was computed over — its IDF terms come from that table's corpus statistics — so a note score and a task score are not comparable numbers and interleaving them produces an order that means nothing. Any single list a UI shows is a presentation decision above this tier with its own stated rule, not a ranking claim. | **Closed** |
| 62 | 2026-09-13 | data-model §A.4 vs §A.5 | **What is a journal's `fts_notes.title`?** §A.5 indexes journals in `fts_notes`, whose second column is `title`, while §A.4's `journal_entries` projection has no `title` column at all. The indexer has nowhere obvious to read one. | None. | §A.5 now says the title is read from `sync_items.payload`, which is where §13.7.2 puts it, with a **fallback to the `date` when the key is absent** — and names why the fallback is the common path rather than an error path: §13.7.2 (defect 59) settled that a newly created day carries no `title` at all, so a journal indexed with an empty title would be unfindable by name for every day the user never titled. | **Closed** |
| 63 | 2026-09-13 | data-model §A.3 | **`source_seq` was given a purpose and never a definition.** §A.3 said it "exists so a rebuild can be skipped when nothing moved" without saying what it counts, and chapter 07's update log has **two independent sequence spaces** (`<id>` and `local.<id>`), so the obvious single maximum sits still while the lower half advances. | None. | §A.3 now defines it as the **sum** of the two namespaces' high-water sequence numbers, with the failure the sum avoids spelled out: a rebuild skipped on a stalled watermark is a note whose search text silently stops tracking its content. Each half is non-decreasing, so the sum moves whenever either does. | **Closed** |
| 64 | 2026-09-13 | data-model §A.3 | **`note_bodies.text` had no owner.** Every artefact said it was rebuildable and none said which component builds it, so it was a projection nothing in the tree would ever have populated. | None. Found by implementing T131 and discovering the column had no producer. | §A.3 now states the **search index pass materialises it** — `extract_text(doc)` is the only producer the core has and the index pass is the only component that walks every document — writing `text`, `text_sha256`, `source_seq` and `materialised_at` together and leaving `seed_markdown` alone. Recorded with the general lesson: "rebuildable" says when a projection may be thrown away and never says who builds it, and a projection with no owner is one that is simply never populated. | **Closed** |
| 65 | 2026-09-13 | data-model §A.5 | **Which column does the incremental reindex watermark compare against?** §A.5 required a per-table watermark and never named the column. The obvious choice, `sync_items.updated_at`, is wrong in a way that is invisible until it bites. | None. | §A.5 now states `sync_items.updated_at` **MUST NOT** be the watermark column, because it carries the _server's_ instant for a pulled record and so is not monotone in this device's clock: a record the server stamped earlier than the last index pass lands below the watermark, is never indexed, and the note is simply not findable. The columns are the ones this device's own clock writes — projection `synced_at`, `yjs_updates.created_at`, `yjs_snapshots.compacted_at` — compared with `>=` rather than `>`, so a row written inside the watermark's own millisecond is re-indexed rather than missed. Re-indexing is idempotent; missing is not. | **Closed** |

| 66 | 2026-09-13 | FR-057, data-model §A.4 | **No artefact defines task-view membership, and desktop has more than one answer.** FR-057 asks for "the same membership semantics as desktop" for today, upcoming, by-project and completed. Desktop has **three** "today"s (the sidebar's `getFilteredTasks`, the page's `getTasksInDueWindow('today')`, and main-process SQL `getTodayTasks`, which has no overdue and no start date), **two** "upcoming"s (the sidebar's strictly-tomorrow-through-+7 versus the `next7` tab's today-through-+6 with overdue first — and the sidebar arm is unreachable from the UI), and **three** "completed"s (status `type === 'done'`, `completedAt != null`, and due-window-scoped). A port cannot satisfy "the same as desktop" against three answers. | `apps/desktop/src/renderer/src/lib/task-utils/{task-view-helpers,task-status-helpers,task-date-utils}.ts`, `pages/tasks.tsx`, `apps/desktop/src/main/database/queries/tasks.ts`, `packages/db-schema/src/schema/tasks.ts` by the subagent, which reported all three families rather than picking one silently. | New data-model **§A.4.1** names the renderer view helpers as the normative family — that is the surface FR-057 is about — and writes the rules out: live and non-archived only; **completion is the status, not `completedAt`**, with an unresolvable status counting as **incomplete** so a task whose project has not been pulled still appears rather than vanishing; subtasks ride along with a matched parent except in by-project; today is overdue-or-due-today plus started-and-unfinished; upcoming is strictly tomorrow through +7; order is `position` with `id` breaking ties. The caller supplies `YYYY-MM-DD` because the core has no time zone. That desktop's own answers disagree is recorded as a defect against desktop, not resolved here. | **Closed** |
| 67 | 2026-09-13 | 06 §6.7 | **What happens to unknown keys on the _merge_ branch?** §13.2 rule 3 covers a local edit and keeps them. A merge has two payloads and has to choose whose unmodelled keys survive, and no chapter said. | None. | New §6.9.2. The merge **keeps the local copy's unmodelled keys and does not carry the remote's** — the generalisation of §6.7's "a field absent from the list is not merged at all". The remote key is not lost: the sender still holds it and it arrives through the listed fields the moment a build that models it adds them. Carrying it would make a merge silently import state no clock arbitrated. | **Closed** |
| 68 | 2026-09-13 | 06 §6.3.1 | **Does a tombstone go through the field merge?** §13.7.2 says deletes never reach a parser, but chapter 06 never drew the consequence, so a port could reasonably run the document gate and the per-field decision on a delete. | None. | New §6.9.2 states a tombstone **bypasses the merge entirely** — no `fieldClocks` read, no document gate, no per-field decision — with the failure it prevents: a deleted row has no fields, so a merge attempted on one fails to parse a payload that is legitimately absent and records a delete as corrupt. | **Closed** |

| 69 | 2026-09-13 | 12 §12.1 | **§12.1 stated flatly that `seed-from-markdown` does not exist** — "that name appears in planning documents and is a proposed rename; the production field is `doc-load.seedMarkdown`". T124 makes the message real, and it is **not** a rename: both paths are now live and behave differently on purpose, so the chapter's direction table and that paragraph were both wrong the moment T124 landed. | `packages/contracts/src/webview-bridge.ts` and `packages/editor-web/src/main.ts` by the subagent, to establish what the production bridge actually carried. | §12.1's direction table now has **four** rows, splitting markdown → document into the verbatim path and the frontmatter-splitting one, and new **§12.1.0** tabulates the difference — frontmatter, when each fires, what answers it. It also states why the new path **discards** frontmatter rather than reading it: a new note's `tags` and `properties` come from the note record, and a guest that re-derived them would be a second source of truth disagreeing with the record the moment the two were written from different inputs. | **Closed** |
| 70 | 2026-09-13 | 12 §12.1.1 | §12.1.1 says normatively that the guest performs "**no frontmatter split**". That remains true of `doc-load.seedMarkdown` and is **false** of `seed-from-markdown`, which T124 requires to split — so the section forbade the thing the task mandated. | None beyond the task text and data-model §A.3. | §12.1.1 now states the frontmatter split as **the one exception, and only on `seed-from-markdown`**, pointing at §12.1.0's table for the difference, and records that the splitter is shared rather than reimplemented — one implementation in the tree. | **Closed** |
| 71 | 2026-09-13 | T124's own premise, 12 §12.4.1 | **T124 instructed the guest to split frontmatter "with the shared splitter already in the bundle (`@memry/shared`)". No such splitter existed** — not in `@memry/shared`, not in the bundle. The only one in the tree was `splitFrontmatterBlock` in `packages/app-core/src/markdown.ts`, which cannot travel into the bundle because that module imports gray-matter, a Node-only dependency. | `packages/shared/src/index.ts`, `packages/shared/package.json`, `packages/app-core/src/markdown.ts` by the subagent, which reported the premise as false rather than writing a second splitter to satisfy it. | The function moved **unchanged** to `packages/shared/src/frontmatter-split.ts`, dependency-free, and `@memry/app-core` re-exports it — so every existing caller is untouched and there is exactly one implementation. §12.1.1 records the move and the reason. A second splitter would have been the worst outcome available: §12.4.1's byte-exactness claim is a property of the function, and two copies drift. | **Closed** |
| 72 | 2026-09-13 | 12 §12.4.1 | **Chapter 12's line citations into `packages/app-core/src/markdown.ts` went stale** when the splitter moved: `:21-40` pointed at the splitter that is no longer there, and eight further citations in §12.4.1 to §12.5 shifted. A stale citation in a chapter that is the source of truth is worse than no citation, because a reader follows it and reads the wrong function. | `packages/app-core/src/markdown.ts` and `packages/shared/src/frontmatter-split.ts` by the orchestrator, which re-derived each line range from the current files rather than applying the subagent's reported offset. | All nine citations re-pointed and verified against the current files: the splitter now cites `packages/shared/src/frontmatter-split.ts:17-60` with the app-core re-export, and the eight app-core ranges are corrected individually rather than by a uniform shift. | **Closed** |

| 73 | 2026-09-13 | tooling, `.husky/pre-push` | **The pre-push guard against a stale mobile editor asset could only ever fail, and had never once run.** It invoked `pnpm --filter @memry/mobile editor:check`, but T012 moved the editor package out of `apps/mobile` into `packages/editor-web` and took the `editor:build` / `editor:check` scripts with it — `@memry/mobile` has had no `editor:` script since. pnpm answered `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT` with a non-zero exit, which the guard reported as "mobile editor asset is stale". The guard fires only on a push touching `packages/contracts/src/webview-bridge.ts` or the editor sources, which is why it went unnoticed: **T124 is the first change since T012 to touch one**, and it was blocked by a guard reporting a failure it could not have detected. The guard was introduced in `a4756d520`, whose subject is "rebuild the editor asset main has been shipping stale" — the check added to stop that recurring has never been able to run. | `apps/mobile/package.json`, `packages/editor-web/package.json` and `git log -S` on the hook, by the orchestrator, rather than reaching for `--no-verify`. | Filter corrected to `@memry/editor-web` in the hook and in its failure message, with the reason recorded inline so it is not "fixed" back. The same stale invocation was corrected in the three other live callers T012 left behind: `paseo.json`'s worktree setup script (which would have failed outright on a fresh worktree), `DESIGN.md`'s platform-check guidance, and the module comment in `packages/contracts/src/webview-bridge.ts` that names the gate. The `specs/001-mobile-app/` references are left alone: they are the completed feature's record of what was true then. Verified by running `pnpm --filter @memry/editor-web editor:check` → `editor-web asset is current (e23842bed496cdac)`, exit 0. | **Closed** |

| 74 | 2026-09-13 | 06 §6.9 | **§6.9 states no winner rule of its own.** It gives settings a key space and names the single-clocked sub-objects; §6.9.1 gives the write rules. Neither gives the **arbitration** rule, so a port must infer which comparison decides a path — and an inferred rule is one two ports can infer differently. | None. | New **§6.9.0** states normatively that a settings path is arbitrated by §6.3's rule unchanged — tick sum, the asymmetric `_offline` tie-break, §6.4.2's canonical comparison, all per path — and that the merge unit set is the **union of both payloads' `fieldClocks` keys**. That last part is what makes the single-clocked sub-objects work without a table: the unit is whatever key the writer declared, so `sidebar.sectionOrder` arrives as one key holding the whole list. A port deriving the unit from a hard-coded path list would drift from the writer the first time a setting was added. | **Closed** |
| 75 | 2026-09-13 | 06 §6.9.1 vs §6.3.2 row 10 | **The settings analogue of §6.3 step 8 was unstated, and the obvious reading is wrong.** Row 10 says a winner of `undefined` leaves the column untouched. Applied to settings that makes a **ticked removal unable to remove** — the exact thing §6.9.1 requires it to do. The two devices then diverge permanently: the peer's stale value wins the next pull, the clearing device re-clears, forever, in violation of FR-002. | None — found by implementing the merge and by a deliberate break-test. The subagent's first end-to-end test covered only the clearing seat, where local wins and the value is already gone, so it passed under the broken behaviour; it strengthened the test to cover the receiving seat, which then failed correctly. | §6.9.0 states normatively that **when the winning side has no value at that path, the path is REMOVED**, and says why settings diverge from row 10: §13.4's absent-means-"the sender does not model this" holds for a task field, but §13.2 makes a settings payload carry every preference its sender holds, so an absent path is a removal. The permanent-divergence consequence is spelled out, and the tick and the removal are named as one mechanism a port MUST implement both halves of. | **Closed** |
| 76 | 2026-09-13 | 06 §6.9 | **§6.9 does not say what a `fieldClocks` key that is not an addressable path does** — `""`, or one with an empty segment like `general.`. A port must decide between refusing the payload and ignoring the key. | None. | §6.9.0 states such a key **rides along as a clock and arbitrates nothing**, deriving it from §6.9's existing rule that a key outside the modelled set must ride along rather than fail the payload: an unaddressable key is that rule's limiting case, and refusing a payload over one would stall every other synced setting. | **Closed** |

| 77 | 2026-09-13 | 12 §12.5, §12.6, §12.7 | **No section anywhere described the XML layout inside the `prosemirror` fragment.** §12.6 and §12.7 give the _markdown spelling_ of blocks and inlines; §12.5 gives the roots. Nothing gave the element nesting, the attributes, or the fragment's top-level shape. A client that only reads a body does not need it — but T125 writes one, and **getting it wrong destroys content silently**: BlockNote's `topNode` is a `blockGroup`, so a `blockContainer` appended as a direct child of `prosemirror` beside that group is not constructible, and y-prosemirror's repair heuristic answers a non-constructible node by **deleting** it. The update applies, the document encodes, `extract_text` may still return the text, and the next desktop to open the note renders it without the block. | `node_modules/@blocknote/core/dist/{yjs.js,blocknote.js,blockToNode-*.js}` and `apps/desktop/src/main/sync/blocknote-converter.ts` by the subagent, which reported it rather than guessing; the orchestrator independently confirmed `topNode: nodes.blockGroup.create()` and the `doc`-parented `blockGroup` check in the same bundle before writing the chapter. | New **§12.5.0** gives the layout as a diagram and makes four things normative: the fragment's single top-level child is a `blockGroup`; a writer MUST locate its parent rather than assume one, creating the group on an empty fragment and **refusing** an unrecognised layout rather than guessing; each `blockContainer` carries a v4 UUID `id` that a writer SHOULD write and a reader MUST NOT fail without; and default-valued `props` are omitted. The silent-deletion consequence is stated in full, because it is invisible at every layer a naive client can check. | **Closed** |
| 78 | 2026-09-13 | `crdt-update.json`, `text-extract.json` | **A committed vector case was named for a shape it does not have.** `crdt-update.json`'s case was called "a real Yjs update over a BlockNote-shaped document" and is `prosemirror > paragraph > text` — no container, no group, which is not a shape BlockNote produces. `text-extract.json`'s `a plain paragraph document` is `prosemirror > blockContainer > paragraph`, also not the real layout. Both are hand-built and byte-correct for what they pin, so SC-001 is unaffected — but the **vectors are the byte-level contract**, and a port reading that case name would build the wrong shape and hit defect 77. | `packages/contracts/scripts/vectors/crdt-update.ts` by the orchestrator. | The case is renamed **`a real Yjs update, shape-agnostic`**, its `pins` now says the node shape is not the real note layout and points at §12.5.0, and the generator's doc comment states the divergence and why the fixture is still correct for what it pins. The bytes are unchanged apart from the case name; `vectors:check` still passes 11 classes and the Rust vector tests are green. §12.5.0 closes by naming both fixtures and stating that **the section, not a fixture, is the authority on what a writer produces**. | **Closed** |

| 79 | 2026-09-13 | 07 §7.15 | **§7.15 does not say which tier owns which obligation, and the omission hid two bugs at once.** It says "delete the local Y.Doc **and** the local update log", citing a desktop handler that holds both a registry and a connection. A core whose apply path holds only a `&Connection` cannot do the first half — so the whole consequence read as optional, and in `memry-core` **none** of it was implemented: the projection row was never marked deleted and `update_log::purge` had no production call site at all. A note deleted on another device stayed visible in every projection-driven read, and because `recent_document_ids` filters on `notes.deleted_at IS NULL`, the next first sync re-pulled the deleted note's body — the exact resurrection the section's second consequence forbids, reached by a client that believed it was obeying it. | None — found by writing T134's seam test and confirmed by the orchestrator against `sync/store.rs`, `first_sync_store.rs` and a `grep` for `purge` call sites. | New **§7.15.1** splits the obligations by tier in a table: three **durable** ones that MUST commit **as one transaction**, and one **runtime** one. Doing them as separate statements leaves a crash between them showing a record marked deleted with a live body log — the forbidden state, reached by trying to obey the rule. A component that cannot complete the purge MUST fail the whole delete, since a delete is idempotent and arrives again next pass while a half-applied one does not. And a component holding no registry MUST **report the purged ids** rather than skip the runtime obligation silently. | **Closed** |
| 80 | 2026-09-13 | 07 §7.15 vs 05 §5.12.1 | **§7.15 never mentions the untyped tombstone, and the interaction is a data-loss hazard.** §5.12.1 says an untyped delete marks every row for an id whatever its type, and that a client MUST NOT infer a type from an id shape — `tag_definition` ids are tag names, `folder_config` ids are folder paths, so the id spaces are not disjoint. Chapter 07 says to purge the update log on a tombstone without saying _which_ tombstones. A port that purged on any delete would let **a tag whose name matches a note id destroy that note's body**, unrecoverably: §7.15 leaves the server rows, so nothing errors and the device just stops asking for them. | None. | New **§7.15.2**: purge only for a tombstone whose **item type** is a document type, never by id shape, with the collision spelled out and the word _unrecoverable_ used. The untyped tombstone is named as the deliberate exception that purges unconditionally, with the reason it is not id-shape inference — an untyped delete marks every row under that id, so whatever owns the body is among the things being deleted. | **Closed** |

| 81 | 2026-09-13 | tasks.md T080, `contracts/core-api.md` | **T080's round trip is unreachable as written.** It asks for a Swift `Transport` "called from Rust for a round trip to staging `/health`", and **nothing in the exported FFI surface issues `GET /health`** — the only Rust-issued requests are `AuthSession`'s `/auth/*` routes, and `/health` appears in the core solely in `crates/memry-core/tests/http_client.rs`. A subagent following the task literally would either invent an export to satisfy it or quietly test something else and call it the same thing. | `crates/memry-core/src/protocol/{auth,http}.rs` and `src/api/auth.rs` by the subagent, which reported the gap and **refused to invent an export**, evidencing the foreign-trait hop against a reachable route (`POST /auth/otp/request`) and labelling the `/health` leg as shell-driven. | T080's text now names a **reachable** route for the Rust-driven leg and records that `/health` is shell-driven because no exported path reaches it. Growing the FFI surface an unauthenticated probe stays available as a **decision for Kaan**, explicitly not a task: the surface is the contract that `core-api.md` and the committed bindings pin, and inflating it to satisfy a spike's wording is how a surface grows without anyone deciding it should. | **Closed** |
| 82 | 2026-09-13 | research.md R10 | **R10's admissibility condition for the `WKURLSchemeHandler` fallback is not met by the fallback as configured, and R10 does not say what to do about it.** It admits the fallback only if the three storage APIs "throw or are unavailable". Measured on iOS 26.5: with `websiteDataStore = .nonPersistent()` under a custom scheme, `localStorage`, `sessionStorage` and `indexedDB` all **resolve** — exactly the cost R10 predicted but did not resolve. R10 is silent on whether a JavaScript-level denial satisfies the condition or whether the platform guarantee is required. | None outside the spike's own Swift. Found by running the measurement rather than reasoning about it. | R10 now records the measurement and the distinction it forces: the opaque origin denied storage as a **platform guarantee**, whereas the fallback needs a `WKUserScript` at `.atDocumentStart` removing the three APIs — a **JavaScript-level denial**, which anything running before it or outside it can defeat. The condition is recorded as **met only with that script**, and the weaker guarantee named rather than smoothed over, so the security claim a later reader inherits is the true one. | **Closed** |
| 83 | 2026-09-13 | research.md §E | **§E asks for device screenshots of the keyboard accessory view and does not say what a simulator capture is worth.** T082's blocking decision makes the device tier unavailable, so §E's Liquid Glass question cannot be closed by this spike — but §E gives no rule for partial evidence, leaving a later reader to treat a simulator image as either sufficient or worthless. | None. | §E now states that the simulator captures evidence the **layout** contract — the accessory view renders above the software keyboard, contrast, legibility and hit targets hold under Reduce Transparency — and does **not** evidence device rendering of Liquid Glass, which stays open alongside T082. The spike notes label both screenshots as simulator captures in the same sentence that presents them. | **Closed** |
| 84 | 2026-09-13 | research.md R16 | **R16's simulator gotchas list is incomplete in two ways that each cost a run.** It names the hardware-keyboard trap and omits the QuickPath introduction overlay, which covers the accessory view entirely on a fresh simulator. And `defaults write com.apple.Accessibility ReduceTransparencyEnabled` does **not** reach `UIAccessibility` — the value survives a device reboot in the plist and the app still reads `false`; only driving the Settings switch works. | None. Both were found by running into them. | R16 gains both, with the failure mode each produces: the QuickPath overlay makes a correct toolbar look absent, and the `defaults write` path makes a Reduce Transparency run look like it was taken with the setting on when it was not — which would have put a wrong screenshot into G3a's evidence under a true-sounding caption. | **Closed** |

| 85 | 2026-09-13 | 04 §4.8 vs §4.6, §4.10 | **Nothing said a record push must not _sign_ `stateVector`, only that it must not _send_ it — and the difference is a total failure.** §4.6 forbids sending it; §4.8's bullet says `metadata` carries `clock` and `stateVector` when they exist; §4.10 says the server reconstructs the signature payload **from what it received**. A port following §4.8 literally signs a field the server never sees, cannot rebuild the payload, and gets `403 SYNC_INVALID_SIGNATURE` — not on a malformed item, but on **every** item that happens to have a state vector. The committed `record-envelope` vectors ship a case that signs one, so the vectors do not catch it either. | None — found while writing the production `PushSealer`, by reading §4.6, §4.8 and §4.10 together rather than in isolation. No TypeScript was read. | §4.8 now states the rule outright and says why it is not derivable from the bullet above it: the bullet is correct for the **general** writer, which is what the vectors pin, and the push writer is a narrower one. The failure is named as total and as looking like a key problem rather than a payload-shape one. The chapter also records the safer construction — a write path whose row has no state-vector column satisfies the rule structurally, which is what `memry-core` does. | **Closed** |
| 86 | 2026-09-13 | 06 §6.6 | **§6.6's "never emit `_offline` on the wire" does not say whether the payload's embedded clock is in scope**, only the envelope's. A port must decide whether to rewrite the ciphertext's contents on the push path. | None. | Recorded as answered by the surrounding rules rather than by §6.6 itself: the envelope clock is rebound and stripped, and the **payload bytes are pushed unchanged**, because §13.2 rule 1 and §6.5.2 P2 give the push path no licence to rewrite them. A payload written by a desktop that hit #2179 therefore still carries `_offline` inside the ciphertext, and that is correct — rewriting it would be the re-serialisation those rules exist to forbid. Noted in the seal implementation with the citation; §6.6's normative sentence is about the clock the server reads. | **Closed** |
| 87 | 2026-09-13 | 05 §5.4 | **§5.4 states the server's clock requirement and not what a client should do with a clock-required row whose live clock is missing.** The options — send it and let the server reject it forever, or refuse locally — differ in whether a queued write is silently dropped. | None. | Recorded: the seal **refuses**, which makes the coordinator retire the row with the reason recorded per data-model §A.2, rather than queueing an item the server will reject on every pass until the outbox is cleared by hand. Flagged in the implementation as the arm that **drops a queued write**, so it is visible rather than discovered later; `settings` is the one clock-exempt type and is excluded from the check. | **Closed** |

| 88 | 2026-09-13 | 12 §12.5 | **No chapter says a whole-fragment replace loses concurrent edits.** Chapter 12 describes incremental edits throughout, for which Yjs's merge is what it claims. A `fragment.delete(0, fragment.length)` followed by a re-seed is a different shape: the deleted items become tombstones, and a peer's concurrent update targeting them merges as an insert into deleted content — it applies without error, both devices converge, and one device's work is silently gone. "Converged" and "nothing was lost" come apart here, and the chapter gave a port no reason to expect it. | `apps/desktop/src/main/sync/{crdt-external-feed,crdt-feed}.ts` by the orchestrator, after **observing the loss on the real staging account**: a `memry-cli` append held unpushed, a concurrent desktop edit to the same note, then the push — the CLI's paragraph was absent from both sides afterwards. | New **§12.5.0.1**, written as a warning rather than a permission: a client MUST NOT use a whole-fragment replace as its merge path for remote input, and where one re-seeds from an external source of truth it MUST be treated as an authoritative overwrite rather than an edit, with a concurrent peer edit expected to be lost. Records the desktop observation and that `crdt-feed.ts`'s own comment calls it "lossy re Yjs history" — true, and understating it, because the loss is of concurrent **content**. Notes that an edit typed into the editor takes the incremental path and is unaffected. | **Closed** |

| 89 | 2026-09-13 | tasks.md T141, research.md R15, `contracts/shell-seams.md` | **T141 and R15 name "the Rust-to-Swift event foreign trait" in the definite singular, and no such trait exists.** `crates/memry-core/src/seams/` exports twelve traits. Nine are implemented in Swift and called from Rust (the eight seams plus `SocketHandle`); three are implemented in Rust and called from Swift (`LifecycleObserver`, `ReachabilityObserver`, `SocketListener`). **Every one of the nine is request-shaped** — the core asks, the shell answers — and none is a core-to-shell event channel. Meanwhile `shell-seams.md` says the list is **closed** and a ninth seam requires a written justification. A subagent following T141 literally has only two moves, and both are wrong: invent the ninth seam without the justification the contract demands, or implement something else and call it by the task's name. That is spec-defect 81's shape recurring, one band later. | `crates/memry-core/src/seams/{mod,transport,editor,reachability,background}.rs` and the generated `packages/swift/MemryCore/Sources/MemryCore/Generated/` protocol list, by the orchestrator, before dispatching the wave. No `apps/desktop` or `packages/sync-client` was read. | New **§There is no event seam** in `shell-seams.md` states the implementer split as a table, says outright that none of the nine is an event channel and that a shell **must not add one**, and replaces the missing trait with the rule that was the point all along: **a Swift method Rust calls MUST NOT re-enter the core**, because UniFFI dispatches it synchronously on the calling Rust thread while a registry or connection lock may be held — a deadlock that only appears under the interleaving that produced the callback, which is why it survives a test suite. `CoreEvents.swift` is named as the enqueue target, and `.bufferingNewest(256)` is justified rather than asserted: every element is a **hint** and the UI re-reads a snapshot through the executor, so dropping the oldest costs nothing; a stream of state deltas could not be lossy. T141's task text is rewritten to match, and a band-B3 emitter is recorded as a change to the closed list requiring the written justification. | **Closed** |

| 90 | 2026-09-13 | research.md R15, `contracts/core-api.md` | **R15 says "core exposes synchronous blocking functions" and T140 says the executor fronts "every core call". Neither is true of `AuthSession`, and `core-api.md`'s band-B2 table did not say so.** Seven of its nine methods are `async throws` in the generated Swift — `refresh`, `registerDevice`, `renewSetupToken`, `requestEmailCode`, `resendEmailCode`, `signOut`, `verifyEmailCode`; only `state` and `markRevoked` are synchronous. Taken literally, a shell wraps a suspending call in a semaphore on a serial-queue thread and **stalls every other core call behind a network round trip** — and because the table listed the methods without marking them, T147, T148 and T152 would each have discovered it separately from the generated Swift and answered differently. R15 was also silent on `Task` cancellation against a `withCheckedThrowingContinuation` fronting an uninterruptible blocking call, so that policy would have been invented per-caller too. | `packages/swift/MemryCore/Sources/MemryCore/Generated/memry_core.swift` lines 629–695 by the T140 subagent, independently re-read by the orchestrator before writing this entry. No `apps/desktop`, `packages/sync-client` or `apps/sync-server` was read. | R15 gains two bullets and `core-api.md` gains a paragraph plus a corrected band-B2 row. The rule: the blocking surface goes through the serial queue, the seven async methods are awaited directly, and **which is which is contract rather than an implementation detail**, because the answer decides how every later caller is written. Cancellation is settled in R15 as the only implementable policy — cancelled before the work starts, it never runs and the core is never entered; cancelled after, it completes and the result is delivered, because resuming early leaves a blocking call in flight with nobody holding it — with the requirement that the claim be atomic between the cancellation handler and the queued block, so a late cancel is a no-op by construction rather than by timing. | **Closed** |
| 91 | 2026-09-13 | research.md R15 | **R15's SE-0461 rationale is written in the present tense, is not in force on this toolchain, and the gap silently generates false greens.** R15 argues the `DispatchQueue` is load-bearing because "SE-0461 makes nonisolated async run on the caller's actor". On this toolchain `-swift-version 6` does **not** enable `NonisolatedNonsendingByDefault`, so a `nonisolated async` function still hops to the global concurrent executor. Consequence, observed rather than reasoned: with the `queue.async` hop **deleted entirely**, a `isMainThread() == false` assertion **still passed** — the work ran on `com.apple.root.user-initiated-qos.cooperative`, off the main thread and on no queue of ours. The test agreed with the very bug it was written to catch, and would have kept agreeing until SE-0461 landed and the executor silently stopped working. | None. Found by the T140 subagent deliberately breaking `CoreExecutor.swift` and watching which assertions stayed green, exactly the Phase 3 discipline; it then strengthened the assertion and re-broke to confirm. | R15 records the toolchain fact and the testing rule it forces: a main-thread assertion does **not** evidence the executor, and a test must assert **queue identity** (`__dispatch_queue_get_label(nil)` against the label the executor was built with), because the guarantee was always "this serial queue" and never "some other thread". Also records that `Thread.isMainThread` and `Thread.current` are unavailable from an asynchronous context under Swift 6, so `pthread_main_np()` is the available primitive. The strengthened assertion is in `CoreExecutorTests.swift` and the episode is written into that file's header so the next reader does not re-weaken it. | **Closed** |
| 92 | 2026-09-13 | tasks.md T141, T158 | **T141 defines `CoreEvents` as "the single path from anything asynchronous to SwiftUI" and no task owned the other end of it.** T143 to T146 each produce into the hub, and T158's `SyncStatusView` is the first view whose content is a value the core computed and must therefore refresh on a hint — but nothing named who calls `consume()` or who drives the app-root observer. The hub is single-consumer by decision, so this is not a detail that resolves itself: the **first** task to guess would take the stream, and every later one would get `nil` and either wire a second hub or read an absent stream as "no events". | None — reported by the T141 subagent, which documented the intent in the file and deliberately wired nothing rather than inventing a call site. | T158's task text now **owns the consumer**: the single `consume()` call site is in the app root, which iterates the hub and re-reads snapshots through `CoreExecutor` into `@Observable` state every view reads, and a view must read that state rather than the stream. Names the `nil`-on-second-call behaviour so a later task recognises it as the contract rather than a bug. | **Closed** |
| 93 | 2026-09-13 | research.md R15, `apps/ios/TestPlans/Unit.xctestplan` | **The `Unit` test plan is not hermetic and hangs rather than fails.** `SpikeTests`' S2 case — "the core calls Swift's URLSession and reads staging's answer back" — makes a **live staging HTTP call** with no time limit. When staging is slow or unreachable it hangs the entire plan with no error output. It cost two runs in this wave, and the failure mode is the dangerous kind: an indefinite hang in a plan exercising new concurrency code looks exactly like a deadlock in the code under test, so the natural response is to go hunting for a bug that is not there. | None. Observed twice by the T141 subagent (`nw_read_request_report ... Operation timed out`) and confirmed by the orchestrator reading `apps/ios/MemryTests/SpikeTests.swift:54-56`. | Recorded in R15's gotchas: scope an evidence run with `-only-testing:` rather than running the bare plan, and read a hang there as the network before suspecting the change. The spike's code is left alone deliberately — it is Phase 3 evidence and rewriting it to satisfy a Phase 4 convenience is how evidence stops meaning what it claimed. Giving S2 an explicit time limit, so it fails diagnosably instead of hanging, stays available as a decision rather than a task. | **Closed** |

| 94 | 2026-09-13 | `DESIGN.md` §Copy and trust | **Nothing in the specification says what an error tells a user, and T142 had to write sixty-four sentences with no source.** `DESIGN.md` gives one sentence — "Error copy says what failed and what the user can do next" — with no vocabulary, no worked example, and no rule for the case that dominates the real surface: an error the user **cannot** act on. The chapters specify error _semantics_ exactly (`core-api.md` says precisely which wrong behaviour collapsing each variant produces) and presentation not at all, so every screen from T147 to T159 would have improvised its own register. Several sub-questions had no answer either: whether a swallowed error is "not alerted" or "not known", whether a rate limit's `retryAfterS` or an out-of-space error's byte counts are shown, and whether copy may tell a user to report a problem when **no iOS support channel is documented anywhere in the spec or `PRODUCT.md`**. | None. Reported by the T142 subagent, which wrote the copy under a stated rule and listed every place it had to decide without a source rather than presenting the result as derived. | `DESIGN.md` §Copy and trust gains **§Error copy, in detail**, seven normative bullets. The load-bearing ones are tied to failures already in this log rather than to taste: an unactionable error still gets a second sentence saying what happens _without_ the user; a permanent refusal must never be phrased as a transient one, because a permanently-rejected record wedges the whole outbox and "try again" then means "retry forever"; an unrecognised error is **loud, never blank**, so "could not tell" cannot read as "nothing is wrong"; copy never contains a payload; a duration or byte count the user cannot act on is not rendered; only a user's own cancellation and a spec-mandated silence are silent, and silent means not alerted rather than not known; and no copy promises a reporting channel that does not exist. | **Closed** |
| 95 | 2026-09-13 | tasks.md T142, T147 | **T142 had to invent the shape every later view renders through, and no task ratified it.** No chapter says whether the shell renders an error as a string, a title plus a suggestion, or something carrying a retry affordance. T142 chose `UserFacingError { code, title, guidance, recourse, isUserVisible }` — defensibly, since two contract distinctions (`RefreshBlocked` versus `SessionExpired`, a 503 versus a 400) are exactly a three-valued recourse that a `Bool` collapses. But **T148 through T159 all inherit it**, and a shape that does not serve a real screen gets worked around eleven times instead of fixed once. | None — reported by the T142 subagent as a decision it was forced into, not presented as derived. | T147's task text now names it: T147 is the first screen to render through `ErrorMapping`, and therefore **the task that ratifies the shape** — if it does not serve a real screen, change it there. T147 is also recorded as the first production construction of `AuthSession`, which makes it the first real wiring of T143's `Keychain` and T145's `Transport`, so the two questions land together. | **Closed** |
| 96 | 2026-09-13 | `DESIGN.md`, FR-023 | **`RecoveryError::UnknownWord` carries the offending word and no rule said whether a view may show it.** FR-023 and Constitution II govern **logging** key material; neither governs **display**. A sentence on screen reaches screenshots, screen recordings and support threads, so the omission is not academic — and the natural, helpful-looking implementation ("_chartreuse_ is not a recovery word") puts a seed word in an alert. | None. | Ruled in `DESIGN.md` §Error copy: a recovery-phrase word is **not displayable** in error copy, because it is eleven bits of the seed and an error can say a word is not in the list without repeating it. A view **may** highlight the offending word in the field the user typed it into, since that text is already on screen and under their control — which is the affordance the associated value exists to serve. | **Closed** |
| 97 | 2026-09-13 | `contracts/core-api.md` | **`ApiError::Status` forces the shell to re-derive retryability, and the contract did not say so.** Everything without a distinct variant collapses into `Status { status, code }`. A shell rendering that must split 5xx (and 408) from 4xx itself, or it describes a request the server will refuse every time as a transient one — which lands directly on the wedged-outbox bug, where "try again" means "retry forever". Unstated, each of T147 to T159 would have made that split, or failed to, independently. | None — surfaced by the T142 subagent implementing the mapping and noticing it had to invent the split. | `core-api.md` records the derivation as **deliberate**: the status code is carried precisely so the shell can make that split, and the split's purpose is named with the outbox consequence. Bounded in the same paragraph: the shell owns only the sentence, the core still owns retry, and a shell must not grow a retry schedule off the back of it. | **Closed** |
| 98 | 2026-09-13 | `DESIGN.md`, FR-077, tasks.md Phase 4 | **Every user-facing string T142 wrote is a Swift literal, and nothing in the spec mentions localization.** FR-077 and `DESIGN.md` require RTL, and RTL presupposes localization — yet no Phase 4 task, and no chapter, names a localization catalogue or mechanism for iOS. Left alone, Phase 4 ships sixty-four hard-coded sentences plus every label from T147 to T160, and the rewrite lands after the shape is set. | None. | `DESIGN.md` §Error copy states the rule outright: every user-facing string is localizable, through the platform's mechanism, **from the first line rather than after a milestone**, and a hard-coded literal is a string someone rewrites later. The strings T142 landed are literals today; converting them is a known, bounded follow-up recorded here rather than discovered at T160. | **Closed** |
| 99 | 2026-09-13 | research.md R15 | **A test over an `AsyncStream` that drains a fixed count hangs instead of failing, and nothing warned of it.** T143's first draft awaited N elements and asserted on them. Under the exact mutation the test existed to catch — a **missing** yield — the `await` simply parked: one `xcodebuild` sat for **21 minutes** and printed nothing, and the stall was indistinguishable from a deadlock in the seam under test, which is the single worst failure signal for concurrency work. It was found only because the agent was running break-tests; a green suite would have hidden it, and a later hang would have sent someone hunting for a deadlock that did not exist. | None. Found by the T143 subagent breaking its own assertions, and fixed before reporting. | R15 gains the rule and the reason: **a hang is worse than a failed assertion, because a hang looks like slowness.** Emit a sentinel, `finish()` the stream, drain to completion and compare the **whole** history — then a missing element, a duplicated element and a wrong payload are three distinct failures rather than one timeout. A `.timeLimit` is the backstop, never the mechanism. This is spec-defect 91's family: an assertion that cannot fail cleanly under its own bug is not a test. | **Closed** |

| 100 | 2026-09-13 | tasks.md Phase 4, 07 §7.15.1, §7.13.3 | **Two runtime obligations the specification assigns to "the shell" had no owning task.** §7.15.1 splits the purge by tier and says a component holding no registry MUST **report** the purged ids for a registry holder to act on — `PullReport::purged_documents` and `BodyPullReport::advanced_documents` do exactly that, and **nothing consumes either**. §7.13.3's snapshot cadence is the same shape: `snapshot_is_due` exists in the core and nothing polls it. Phase 4's prompt says the shell is that holder and that this is Phase 4's to wire, but **no T-number claimed either**, so both would have shipped as they are — a deleted note staying resident and visible, a purged one re-pulled on the next first sync, and the snapshot cadence never running. This is the Phase 3 pattern exactly: implemented, tested, reported, and never called. | None — found by the orchestrator reading the Phase 4 task list against the known-bugs list rather than trusting that "the shell will do it" named someone. | T158's task text now owns both, on the same ground that gave it T141's `consume()` call site (defect 92): it owns the **app root**, and the app root is the only thing in the shell that holds a document registry and a lifecycle. One seat owns all three runtime obligations rather than three tasks each assuming another did it. | **Closed** |

| 101 | 2026-09-13 | research.md R5, tasks.md T145, `crates/memry-core/src/seams/transport.rs` | **R5 and T145 both require a large blob fetch to return a file path rather than bytes, and the FFI record has no path.** `HttpResponse` is `{ status, headers, body: Data }` — one field, bytes, no variant. The instruction cannot be obeyed as written, and the two ways to appear to obey it are both wrong: invent a path variant on the record, which is a **surface change** that `core-api.md` and the committed bindings pin, or quietly spill to disk and read the file back into `Data`, which pays the disk cost and still hands the core a fully-resident body. | `packages/swift/MemryCore/Sources/MemryCore/Generated/memry_core.swift:5180-5186` by the T145 subagent, independently re-read by the orchestrator. `crates/memry-core/src/seams/transport.rs`. No `apps/desktop` or `packages/sync-client` was read. | Recorded as **the reachable half implemented and the unreachable half named as a decision rather than a task**, the treatment defect 81 gave T080. The obligation a shell can actually meet is that a body is never accumulated into one growing resident buffer and never exists twice in Swift memory: past 1 MiB the response streams to a file, crosses **memory-mapped**, and the file is `unlink`ed the instant it is mapped, so nothing survives the call. Growing `HttpResponse` a path variant stays available as **Kaan's decision**, explicitly not a task — the surface is the contract, and inflating it to satisfy a task's wording is how a surface grows without anyone deciding it should. | **Closed** |
| 102 | 2026-09-13 | 09 §9.6, `contracts/shell-seams.md` | **Chapter 09 §9.6 requires a literal `ping` as a WebSocket _text_ frame, and `SocketHandle::send` carries only bytes.** The seam takes `Vec<u8>` on purpose — §9's whole point is that the shell must not grow an opinion about the frame — but a WebSocket wire frame is typed, so the shell cannot avoid choosing text or binary, and a keepalive sent as binary is not the frame Cloudflare's request-response pair matches. The chapter gives the payload and is silent on the opcode. | `docs/protocol/09-realtime.md` §9.4 and §9.6, and `crates/memry-core/src/sync/socket.rs`'s `KEEPALIVE_FRAME`, by the T145 subagent. | `shell-seams.md` records the rule under Transport: a shell sends a **text** frame when the payload is valid UTF-8 and a binary frame otherwise. This is a **framing** decision rather than a parse — it inspects encoding, never content — so the shell still cannot form an opinion about the message, and it satisfies §9.6's keepalive and §9.4's JSON envelope by one rule without either being special-cased. | **Closed** |
| 103 | 2026-09-13 | 09 §9.1, `contracts/core-api.md`, tasks.md T145 | **Nothing in the exported surface can open a socket, so T145's socket half is structurally unreachable — and §9.1's "MUST close when backgrounded" has no driver on either side.** `RealtimeClient` is not `#[uniffi::export]`ed: it appears **zero times** in the generated Swift, and its only callers are `crates/memry-core/tests/sync_realtime.rs`. `Transport::open_socket` is therefore a method the core cannot call. `RuntimeHost` records `AppPhase` and nothing links a phase to `RealtimeClient::disconnect()`. This is Phase 3's pattern in its purest form — a tier fully implemented, tested against a real loopback WebSocket server, and unreachable from production by construction — and it is larger than T145. | `crates/memry-core/src/sync/socket.rs`, `crates/memry-core/src/api/runtime.rs` and the generated Swift by the T145 subagent; the orchestrator independently confirmed that `grep -c RealtimeClient` over the generated file returns **0**. | Recorded against T145, which is **ticked for the HTTP half only**, with the socket half named as implemented-and-unreachable rather than done. Exporting a `Sync` object is band B3's, already named in `core-api.md`'s planned-objects table, and the socket cannot be wired before it — not by T147, not by T158. **Phase 4's checkpoint does not need it**: US3 is sign in, unlock, browse, and a realtime hint only shortens the latency of a pull the shell can drive itself. Written down so a later reader finds the gap recorded rather than rediscovering it as a bug. | **Closed** |
| 104 | 2026-09-13 | tasks.md T144, research.md R11 | **T144's verification clause is unsatisfiable on a simulator, and writing it literally produces a guaranteed false green.** It asks that the **effective** class read back from the database and both sidecars equal `NSFileProtectionCompleteUntilFirstUserAuthentication`. Measured: the simulator does not implement data protection at all — `setAttributes(.protectionKey:)` neither throws nor takes effect, `attributesOfItem[.protectionKey]` is `nil` for every file, and `URL.resourceValues(.fileProtectionKey)` returns **exactly that string** for a file set to `.complete`, for a file set to `.none`, and for a file never touched at all. It is the target entitlement's default echoed back, so the assertion passes without the code under test existing. R11 was right and said "re-assert on each sidecar after first open **and test on device**"; T144's text kept the assertion and dropped the device half. | None. Found by the T144 subagent writing the clause literally, watching six class assertions pass, and catching it only by asserting the **wrong** class and watching that pass too. | T144's clause is amended to what a simulator can establish — that the seam **asks for** the class on `data.db`, `-wal` and `-shm`, in that order, **after** the open and on nothing that does not exist, asserted as one ordered history — and the **effective**-class check moves to T161/T162 as device evidence, where R11 always put it. The measurement is kept as a live test rather than a comment, so the next reader sees the platform's answer instead of trusting this entry. | **Closed** |
| 105 | 2026-09-13 | tasks.md T144, T155 | **No task called the `FileProtection` seam, so the sidecars would have shipped unprotected.** The trait has four methods and **zero call sites in `crates/`** — the core opens the databases and never calls back, so "re-assert after first open" is a constraint with no hook in the core at all. On the shell side T147 names wiring `Keychain` and `Transport`, and T155 is where a vault is actually opened (`Unlocked -> VaultChoice`) and mentioned neither the seam nor the post-open sweep. Nothing owned it, and the write-ahead log holds recent note content in plaintext. Defect 100's shape, one wave later. | `crates/memry-core/src/storage/{mod,connection}.rs` by the T144 subagent, which confirmed the absence rather than assuming it. No non-core file was read. | T155's task text now owns the call site and must open every vault through `VaultFiles.openingVault(_:open:)`, whose shape makes the ordering structural — prepare, then the caller's own open closure, then the sweep — so a caller cannot skip the sweep because it sits on the far side of its own closure. Recorded that the sweep runs on **every** open, not only the first: a clean close deletes `-wal`/`-shm`, and the next open creates new files with new attributes. | **Closed** |

| 106 | 2026-09-13 | research.md R15, HANDOFF.md | **`-only-testing:` with a name that matches nothing runs nothing, reports `TEST SUCCEEDED`, and the count looks plausible.** Swift Testing suite names are **struct names, not file names**, and the two diverge freely — `TransportTests.swift` holds `TransportHTTPTests` and `ResponseBodyTests`; `FileProtectionTests.swift` holds `VaultFilesRealFilesystemTests` and `VaultFilesSubstitutedPlatformTests`. Scoping by file name silently selected **none** of them. The orchestrator's own verification run reported **64 passed, 0 failed, result Passed** where the true figure was **102**: 38 tests, including every one of T144's and T145's, never executed, and the run printed no warning about an unmatched filter. Caught only because 64 was too small against the expected sum — a number nobody had precomputed would have passed as evidence. This compounds spec-defect 93, which made `-only-testing:` mandatory in the first place. | None. Found by the orchestrator re-deriving the expected total and refusing a number that did not add up. | Recorded in R15's gotchas and in HANDOFF.md's run-costing list: **derive the suite names from the source** (`grep -hoE '^(struct\|final class) [A-Za-z0-9_]+'` over the test files) rather than assuming they match the file, and **precompute the expected total before reading the result**, because a scoped run's failure mode is a smaller true number rather than an error. `TEST SUCCEEDED` on an empty selection is the same false green as the XCTest counter printing `Executed 0 tests` for a suite that really ran — two different ways for this toolchain to report success for work it did not do. | **Closed** |

| 107 | 2026-09-13 | `contracts/core-api.md`, `contracts/shell-seams.md`, tasks.md T146 | **Six of the eight seams cannot be handed to the core at all, so the shell cannot wire them however well it implements them.** The **only** exported constructor that accepts any seam is `AuthSession.init(transport:secureStore:baseUrl:clientPlatform:device:)`. `FileProtection`, `Reachability`, `CodeCapture`, `Notifications`, `BackgroundExec` and `EditorHost` are accepted by **nothing**, and `SyncEngine` — the Rust type that actually consumes a `Reachability` — appears **zero** times in the generated Swift, as does `RealtimeClient`. T146's task text says `observe` "finally gets a caller" there; it cannot. This generalises defect 103 from the socket to most of the seam list, and it reframes known bug 6: "wire `observe` or the queue waits for a timer" is **not a choice the shell can make today**. | `packages/swift/MemryCore/Sources/MemryCore/Generated/memry_core.swift` by the T146 subagent, which refused to invent an export; the orchestrator independently confirmed a single seam-taking constructor at line 747 and `grep -c` returning **0** for both `SyncEngine` and `RealtimeClient`. No non-core file was read. | Recorded, and the phase re-read against it. **It does not block Phase 4's checkpoint**, and that is the important half: the seams the shell drives _itself_ are all reachable — T155 calls `VaultFiles.openingVault` directly (defect 105), T153 calls `CodeCapture.scan()` directly, and `Keychain` plus `Transport` cross through `AuthSession`, which T147 has now wired and proven. What is unreachable is exactly the set the **core** must hold to drive sync on its own, and that arrives with band B3's `Sync` object, already named in `core-api.md`'s planned-objects table. T146 is ticked for the seam implementations with the Rust-caller half recorded as band B3's, not as an omission. | **Closed** |
| 108 | 2026-09-13 | `contracts/core-api.md`, research.md R15 | **An `async` core call cannot be cancelled from Swift, and nothing said so.** `rust_future_cancel` appears **zero** times in the generated bindings, and `uniffiRustCallAsync` bridges with `withUnsafeContinuation` and no cancellation handler — so cancelling the Swift `Task` around `AuthSession.requestEmailCode` does not stop the call. T140 settled cancellation for the **blocking** surface through `CoreExecutor` (defect 90), and the natural reading was that the async surface behaved like ordinary Swift concurrency. It does not. A shell that offers a Cancel button on a suspended core call is lying to the user, and a shell that races a timeout against one leaks the in-flight call. | The generated bindings by the T147 subagent, independently re-confirmed by the orchestrator (`grep -c rust_future_cancel` → 0). | Recorded alongside T140's policy: the executor's cancellation contract covers the blocking surface **only**, and an async object method is uncancellable once entered. The shell's answer is therefore structural rather than a control — bound the number of calls it can start, and show which wait the user is in — never a Cancel affordance that cannot cancel and never a shell-side timeout that abandons a live call. Compounds defect 109, which is what makes the wait long enough to matter. | **Closed** |
| 109 | 2026-09-13 | 00 §0.6.1, 02 §2.11 | **The retry ladder puts no cap on a server-supplied `Retry-After`, so one call can suspend for hours — and this is what "known bug 9" actually was.** `http.rs` turns a 429's `Retry-After` straight into the delay (`retry_after_s.map(|s| s.saturating_mul(1_000))`) with no ceiling, and `/auth/otp/request` runs the default policy with `retry_on_429: true`. The per-email limiter throws a bare 429, giving the 2/4/8 s ladder — about 14 s, which is the behaviour everyone assumed. But the **per-IP** limiter is 10 requests per **3600 s** and sets `Retry-After` to the **remaining window**, so an eleventh OTP call from one address makes `requestEmailCode` suspend for up to an hour. Phase 3 recorded this as "the fourth request hangs with no output". **It was never a hang**; it was the ladder obeying the server, and with defect 108 it cannot be interrupted. Two sessions were spent treating it as a rate-limit mystery. | `apps/sync-server/src/services/otp.ts`, `src/routes/auth.ts` and `src/middleware/rate-limit.ts` by the T147 subagent — it reported the read rather than guessing, and it is the one question chapter 02 §2.11 could not answer. `crates/memry-core/src/protocol/http.rs:393-404` re-read by the orchestrator. | Recorded as a real gap in §0.6.1 rather than a client bug: a ladder that honours an unbounded server-supplied delay has no way to distinguish "wait eight seconds" from "wait an hour", and the caller cannot tell the difference either because the call is uncancellable. **Capping the honoured delay in the core's ladder is a change to the core and a decision for Kaan, not a Phase 4 task.** The shell's mitigation is what T147 shipped: a hard client-side cap of three code requests per address, so the screen cannot reach the per-IP limiter through ordinary use, and a spinner that names which wait the user is in. HANDOFF.md's known-bug 9 is corrected — it is not a hang, and a run that appears stuck there is waiting exactly as long as the server told it to. | **Closed** |
| 110 | 2026-09-13 | tasks.md T147, `apps/ios/Memry/Resources/Info.plist` | **A release build has no server, and nothing said which file was supposed to name one.** `SYNC_SERVER_URL` comes from a gitignored `.env.<environment>` for the desktop and the CLI, and the iOS target has no equivalent — the base URL must reach the app some other way, and no chapter or task says how. A worktree missing its env file has silently fallen back to `localhost` before, which is the failure mode to refuse. | None. Surfaced by the T147 subagent implementing the resolver and finding no source for the input. | T147's resolver takes three inputs and **has no default**: a `--sync-environment=` launch argument, else an `Info.plist` `MemrySyncEnvironment` key, else — on a release build — `notConfigured`, which renders a full-screen notice and logs at `fault`. `production` is refused on every debug build and from every launch argument. It never falls back to localhost and never falls back to production. **The `MemrySyncEnvironment` key is not in `Info.plist` today**, so a release build is `notConfigured` — deliberately the loud half. Recorded in HANDOFF.md as required before any release or TestFlight build; adding it touches `Info.plist` and the release build settings, both shared files, and is the orchestrator's to land when a build is actually cut. | **Closed** |
| 111 | 2026-09-13 | `DESIGN.md` §Error copy, `apps/ios/Memry/Core/ErrorMapping.swift` | **One sentence per error variant does not survive contact with a second screen, and the first real renderer found two that are wrong.** `ApiError::RateLimited` renders "syncing will resume on its own shortly" — true during sync, false on the sign-in screen, where the user meets it most often (§2.11), where no sync exists and nothing resumes. `AuthError::InvalidState` renders "close this screen and start again" — the sign-in screen is the root, and there is nothing to close. `UserFacingError`'s shape cannot express screen context, and T147 deliberately did not add a context parameter for two variants, which would be a speculative generalisation across fourteen switches. | None. Found by T147 rendering every branch of a real screen, which is exactly what defect 95 made T147 responsible for. | Recorded and **narrowed rather than parameterised**: both sentences are to be rewritten context-neutral — say what happened and that nothing is lost, without promising a mechanism that may not exist on the screen showing it. Added to `DESIGN.md` §Error copy as a rule with teeth: **a sentence must be true on every screen that can show it**, because a mapping keyed only on the error cannot know where it is being read. A context parameter stays available if a third case appears; two is not enough to earn it. | **Closed** |

| 112 | 2026-09-13 | `.github/workflows/ios-ci.yml`, `scripts/check-architecture-boundaries.js`, `apps/ios/.swiftlint.yml` | **iOS CI had been red on `main` since Phase 3 closed — including the commit that declared Phase 3 complete — and nothing recorded it.** Every `ios-ci` run failed, on two independent jobs. Neither `phase4.txt` nor `HANDOFF.md` mentioned it, and the per-file `swiftlint` the task briefs prescribe passes cleanly, so the red never surfaced: the gate everyone ran was not the gate CI ran. Four distinct causes. **(a)** The transport-seam boundary check matched `URLSession` as a plain substring, so the ONE permitted seam implementation — a type named `URLSessionTransport` — reported a violation wherever it was _named_, meaning the check punished exactly the layering it exists to enforce, and it punished prose in comments too. **(b)** `Memry/Spikes/` genuinely called `URLSession` outside the seam, in files compiled into the shipping app: a real second network path the core could not see, retry or kill-switch. **(c)** `SpikeS4Tests.swift` carried two trailing commas. **(d)** `ConformanceTests.swift` and `Vectors.swift` tripped `inclusive_language` on `masterKey` and `nesting` on the vector fixtures. | `.github/workflows/ios-ci.yml` and `gh run list` by the orchestrator, after a project-wide `swiftlint --strict` disagreed with the per-file runs every task had been reporting. | All four fixed, and the fixes were chosen so the gate keeps gating. **(a)** Whole-identifier matching with comment stripping, plus a **both-directions self-test that runs on every invocation** — `URLSession.shared` must be caught, `URLSessionTransport` must be allowed, and a `//` inside a string literal must not hide real code. It lives inside the checker rather than in a test file because nothing under `scripts/` is wired into `pnpm test`, and a test nobody runs is worse than none. **(b)** The spikes were **deleted**, which is what `Memry/Spikes/README.md` itself instructed — "delete the directory … when phase C1 replaces them with the real thing" — and T147 had just replaced the scaffold, so the moment had arrived. This also closes **defect 93** for free: the S2 case that made a live staging call and hung the whole `Unit` plan is gone, and the plan is hermetic for the first time. `SpikeReport` moved into `SpikeS4Tests.swift`, keeping T082's harness alive now that the device exists. **(c)** Commas removed. **(d)** Configured with the reason written down: `master` is the protocol's own term (chapter 01 §1.1, `SecureStoreKey::MasterKey`, `KEYCHAIN_ENTRIES.MASTER_KEY`) and data-model §B requires those names so a desktop and a phone share one vocabulary, so renaming in Swift alone would break a stated invariant to satisfy a lint; the vector fixtures nest `Input`/`Expected` to mirror the committed JSON one-for-one. Project-wide `swiftlint --strict` is now **0 violations in 35 files**, and `check:architecture` passes. | **Closed** |

| 113 | 2026-09-13 | research.md R11, data-model.md §B, `apps/ios/Memry/Memry.entitlements` | **Two facts about iOS data protection that no chapter states, and that a first draft got wrong in both directions.** Measured on the physical iPhone 12 Pro, iOS 27.0, while closing T144's device half. **(a)** The two APIs disagree on prefix: `URL.resourceValues(.fileProtectionKey)` answers `NSURLFileProtection…` while `FileProtectionType.rawValue` is `NSFileProtection…`, so comparing the raw strings across them fails on a **correctly** protected file — a false negative that looks exactly like the bug the test is written to catch. **(b)** Far more important: `com.apple.developer.default-data-protection` is a **floor, not a default**. A file explicitly set to `.none` still reads back as `completeUntilFirstUserAuthentication`, because the entitlement applies to the whole container. Raising still works — `.complete` is honoured. Every chapter treats the entitlement as what _unmarked_ files get, which understates the guarantee the app actually has. | None. Both were measured on hardware after a first draft asserted each of them wrongly and failed; the failure messages were read rather than reasoned around. | Recorded in the test itself, at the assertion, with the measurement and the date — because this is the kind of fact a comment loses and a chapter cannot keep current across an OS release. The stronger claim is now available where it matters: the shell **cannot lower protection below the declared class even by asking**, so a future code path that tried to write an unprotected scratch file inside the container would fail to, which is a guarantee worth knowing before someone designs around its absence. | **Closed** |

| 114 | 2026-09-13 | `contracts/core-api.md` §"Planned objects — band B3", tasks.md T148/T152–T159, spec US3 | **The core exports nothing that the rest of Phase 4 needs, so seven of the eleven remaining tasks have no call site that can be built — and the phase's own checkpoint is unreachable.** The complete generated Swift surface is **10 free functions** (`validate_recovery_phrase`, `derive_master_key`, `derive_vault_key`, `account_key_verifier`, `account_key_verifier_matches`, `local_vault_key_verifier`, `local_device_id_hex`, `compress_payload`, `decompress_payload`, `core_version` — all pure crypto or compression), **2 objects** (`AuthSession`'s nine auth methods and `RuntimeHost`'s five lifecycle methods), and the fourteen seam protocols with their `…Impl` shims. `grep` over the generated bindings returns **0** for `oauth`, `google`, `link`, `pair`, `sas`, `note`, `folder`, and any vault API — the two `Vault` hits are `deriveVaultKey` and `localVaultKeyVerifier`, which are functions over bytes and not a vault. Concretely: **T148** cannot post a provider token (no method accepts one; `AuthState::AwaitingProviderToken` and `AuthEvent::ProviderSheetOpened` are variants nothing can reach, and the request needs a `sessionNonce` the core mints into private state plus a `devicePublicKey` from the signing key inside the core's `SecureStore`); **T152** cannot fetch `{kdf_salt, key_verifier}`, so the unlock screen is wired to a dead input; **T153/T154** have no device-linking surface at all; **T155, T156, T156a, T157** need `Vault` and `Notes`, and **T158/T159** need `Sync` and `Client`. `core-api.md` itself already says these four are band **B3** — "named here so the shape is agreed before the code exists". This is spec-defects 103, 105 and 107 generalised from individual seams to the whole feature: the shell tier can be implemented perfectly and remain unwireable, because there is nothing on the other side. | Reported independently and from opposite directions by the T148 and T152 subagents in the same wave, then **re-derived by the orchestrator directly from `packages/swift/MemryCore/Sources/MemryCore/Generated/memry_core.swift`** rather than taken on report: the exported-object list, the nine `AuthSessionProtocol` methods, the ten top-level `public func`s, and the zero-counts above. `apps/sync-server/src/routes/auth.ts` was read by the orchestrator to confirm that `POST /auth/oauth/:provider/native` really does require two core-private values. | **NOT CLOSED, and deliberately left open.** It cannot be closed by amending a chapter, because the chapters are not wrong — `core-api.md` states plainly that `Client`, `Vault`, `Sync` and `Notes` are planned and unbuilt. What is wrong is Phase 4's task list, which assumes a core surface that a later band supplies. The documentation half **is** done: the B3 table now names, per object, the Phase 4 tasks it blocks, so the next reader cannot re-derive this from the bindings a fourth time. The remaining half is a scope decision — build the B3 exports first, or land Phase 4's shell tiers dark and defer US3's evidence — and that is **Kaan's**, not a task's. Recorded as the log's first open entry rather than smoothed into a closure, because an entry that is not fixed is not closed. | **Kaan decided, 2026-09-13: build band B3's minimum first.** Not the whole band — only what US3's checkpoint needs, leaving the rest of `Client`, `Vault`, `Notes` and `Sync` for their own train. The decision was cheap to make because the orchestrator established, before asking, that **the behaviour already exists and is already tested**: `protocol/account.rs` implements `GET /auth/key-verifier` and `GET /sync/vaults` today, and `domain/notes.rs`, `domain/folders.rs`, the storage repositories and `sync/engine.rs` are what the `memry` CLI already drives. What is missing is the UniFFI layer over them, not the logic under it — so this is an export task, not a protocol build. Carried by two new tasks: **T163** (account key material, vault registry, and a native-OAuth method accepting a Google ID token per chapter 02 §2.13) and **T164** (read-only `Vault` and `Notes`: open, list folders, list notes, read one note). Kaan also ruled that **T148 and T152 stay unticked** although both are committed and fully tested, because a `[x]` means the verification passed and this phase's done means a production call site — which neither has until T163 lands. **Reopen this entry if the export layer turns out to need behaviour that is not already there**; the whole basis of the narrow choice is that it does not. | **CLOSED, 2026-09-14.** Every missing capability now exists **and has a production call site**, which is the standard this entry itself insisted on. T163 exported the account key material, the vault registry and native OAuth; T164 the read-only `Vault`/`Notes` slice; T235 device linking's new-device half. The callers followed: T165 wired the unlock screen and the Google button, T155 the vault picker, T156 the browse surface, and **T153/T154 the QR and SAS screens — which is what finally closed it**, because T235 deliberately left this entry open on the grounds that an export with no caller is not done. **What the entry is really worth keeping for is the pattern it exposed**: work specified against a capability nobody had checked existed, which happened **five times** in one phase — 114, 124, 125, 127 and the cut T156a. Every one would have been caught by a `grep` over the generated Swift before a brief was written. That check is now the first line of HANDOFF.md's guidance. |

| 115 | 2026-09-13 | `.github/workflows/ios-ci.yml`, `MemryTests/SpikeS4Tests.swift`, `MemryTests/FileProtectionTests.swift`, `MemryTests/TransportSocketTests.swift` | **iOS CI was still red on `main` after defect 112 was believed to have closed it, and three of the four remaining causes were false reds that a doc comment described as skips.** **(a)** `try #require(Self.isDevice, …)` is **not** a skip. Swift Testing's `#require` records an _expectation failure_ when its condition is false, so both device-gated tests **failed on every simulator run** while their doc comments said "skipped rather than vacuously passed off-device" — the comment and the code disagreed, and the comment is what everyone read. This is the same family as defect 104 inverted: 104 was a test that passed where it should have skipped, 115 is a test that failed where it should have skipped. **(b)** The workflow passed `CODE_SIGNING_ALLOWED=NO`, so the simulator app carried **no `keychain-access-groups` entitlement** and every `SecItem*` call returned `errSecMissingEntitlement` (-34018), failing all seven `KeychainRealStoreTests`. Those are the only tests that prove data-model §B's identities against the **real** keychain, and they had never executed on CI once. **(c)** The simulator branch of the data-protection measurement pinned a literal `NSURLFileProtection…` string and pinned a _directory's_ read-back to `nil`; the local simulator and the CI simulator disagreed about the directory, so the test asserted a property of one simulator build rather than of simulators. **(d)** The loopback listener's 5 s readiness bound was measured on a warm laptop and flaked on a cold runner, where it is the first test of a `.serialized` suite. | The orchestrator, reading `gh run view --log-failed` on `main` rather than trusting HANDOFF.md — which recorded (a) as "fails on a simulator by design — not a regression, not yours", a sentence that normalised a permanently-red gate. | Closed. **(a)** both sites are now `.enabled(if:)` traits, which genuinely skip; the distinction is written at each site so the next author cannot reach for `#require` again. **(b)** ad-hoc signing (`CODE_SIGN_IDENTITY=-`, `CODE_SIGNING_REQUIRED=NO`, `CODE_SIGNING_ALLOWED=YES`) with provisioning off and no team — which is what Xcode itself does for a simulator build — so the entitlements are embedded without a development certificate the hosted runner does not have. **(c)** re-asserted as **invariance** — all three read-backs are identical, so the value carries no information about what was asked — plus a non-nil check so "could not tell" cannot satisfy it by making all three `nil`. **(d)** 20 s, with the bound kept so a listener that never binds still fails loudly instead of hanging. Evidence: full `Unit` plan, iPhone 17 simulator, run with the workflow's own signing flags — **196 total, 194 passed, 0 failed, 2 skipped**, the two skips being exactly the tests from (a); total precomputed from the `@Test` count before the result was read. The standing rule is reinforced rather than replaced: **run a bare project-wide `swiftlint lint --strict` and check `gh run list --workflow=ios-ci.yml` before believing `main` is green.** | **Closed** |

| 116 | 2026-09-13 | 02 §2.12 | **Chapter 02 does not document the native OAuth endpoint at all, so the one task that had to call it read the server.** `POST /auth/oauth/:provider/native` is the mobile counterpart of the browser callback and nothing in `docs/protocol/` describes its request body, its response, or its failure modes. The question was: what does it accept and return? The answer, read from the source: it accepts `{ idToken, sessionNonce, devicePublicKey }`, rejects any provider but `google` with a `400 AUTH_INVALID_PROVIDER`, answers `501` when `GOOGLE_IOS_CLIENT_ID` is unset — **loud rather than falling back to the web client**, because validating against the wrong audience would accept a token minted for a different app — and returns `{ success, isNewUser, needsSetup, setupToken }`, where `needsSetup` is `!user.kdf_salt`. The route's own comment is **also wrong**: it says "the app signs in with Google's own iOS SDK and posts the resulting ID token", and research R14 forbids that SDK. | `apps/sync-server/src/routes/auth.ts:435-492` read by the T148 subagent, which reported the read rather than guessing; **re-read independently by the orchestrator** before anything was written into the chapter. | Closed by writing the shape into chapter 02 §2.12 rather than into a Swift comment, together with the fact that matters most for defect 114: **two of the three request fields are core-private.** `sessionNonce` is minted into the core's pending state and `devicePublicKey` derives from the signing key inside the core's `SecureStore`, and the returned `setupToken` is signed **over that nonce** — so the request cannot be assembled by a shell, and "the shell posts the ID token" was never a possible reading. The stale SDK comment in the route is noted in the chapter as inaccurate; editing `apps/sync-server` is out of Phase 4's scope by Kaan's scope decision, so it is recorded rather than fixed. | **Closed** |

| 117 | 2026-09-13 | 02 §2.12, `apps/ios/Memry/Resources/Info.plist` | **Nothing says where the iOS OAuth client id lives on the client**, so T148 had to invent a name for it. The server's `GOOGLE_IOS_CLIENT_ID` is written down; its client-side counterpart is not named in any chapter, in `Info.plist`, or in `compliance.md` — and the redirect URI scheme is _derived_ from that id (the reversed client id), so an app that cannot find it cannot even build its authorize URL. This is the same shape as defect 110, where a release build had no server because no file was named as the source of the base URL. | None. Surfaced by the T148 subagent implementing the configuration and finding no source for the input. | Closed by naming it: **`MemryGoogleClientID` in `Info.plist`**, recorded in chapter 02 §2.12 beside the route, with the same discipline T147 established for `MemrySyncEnvironment` (defect 110) — **no default and no fallback.** An absent key throws `notConfigured` and is asserted against the real `Bundle.main`, so a build shipped without it fails loudly rather than authenticating as the wrong client, and it never falls back to the web client. Like `MemrySyncEnvironment`, **the key is not in `Info.plist` today**; both must be added before any release or TestFlight build, and both are the orchestrator's to land when a build is actually cut. | **Closed** |

| 118 | 2026-09-13 | `DESIGN.md` §Color and tint | **`DESIGN.md` mandated the tint for "focus emphasis", and that mandate fails the accessibility floor `DESIGN.md` itself sets.** The default tint `#f97316` is **2.80:1** against the white canvas. WCAG SC 1.4.11 requires **3:1** for a non-text boundary carrying state, and FR-077 requires AA, so a tint-coloured focus ring was non-conformant on the default theme — on every platform, not only iOS. The general form is the important half and nothing in the document said it: **a colour the user chooses can never be what a contrast requirement rests on.** `--tint` is explicitly any valid six-digit hex, so some user picks one that fails against some surface, and no derivation (`--tint-ring`, `--tint-border`) repairs that. A related instance: `--tint-foreground` is assumed white, and white on `#f97316` is also 2.80:1. | Measured by the T160 subagent while resolving `DESIGN.md`'s colour roles into iOS values, and reported as a contradiction rather than worked around; the orchestrator re-read §Color and confirmed the mandate was in the normative list at line 162. | Closed by **splitting the role** in `DESIGN.md` rather than by lowering the floor or by exempting iOS: the tint **fills**, and never carries contrast. Focus rings, focus outlines and any state-bearing boundary derive from **ink** and must clear 3:1 against every surface they can appear on — **including the pressed and selected fills**, which are the hardest backgrounds and the ones a first implementation forgets. The tint stays correct for selection backgrounds, active-tab underlines, checked controls and drag placeholders, where the information is also carried by position, shape or a label. `--tint-foreground` must be computed against the resolved tint. iOS ships this split (`Tint.base` fill-only, `Line.focus` ink-derived, and the word-review highlight moved off the tint). **Desktop is affected and is NOT fixed**: `--tint-ring` and `--tint-border` exist to paint boundaries from the tint, and §Tabs' active-tab underline is a `2px` tint rule. Recorded in `DESIGN.md` as owed work rather than silently left. | **Closed for iOS; desktop owed** |

| 119 | 2026-09-13 | `DESIGN.md` §Typography | **`DESIGN.md` names four font families for mobile that iOS does not have, and gives no mobile type ramp.** The named families are Inter, Space Grotesk, Crimson Pro and JetBrains Mono. `apps/ios` bundles **no font files at all** — `find apps/ios -iname '*.ttf' -o -iname '*.otf'` is empty — while the same section requires respecting Dynamic Type and platform text metrics, which a bundled static family does not do by itself. The type scale given (9–42px) is explicitly desktop, in fixed pixels, and Dynamic Type has no fixed sizes to map onto. | The T160 subagent, which needed a typography answer to build the ramp and found the document naming files that are not in the target. | Closed as a **decision recorded rather than a value invented**: iOS maps the four _roles_ onto system families — SF for the working sans, SF semibold for structural display, **New York** (`design: .serif`) for editorial, SF Mono for mono — and the ramp is seven Dynamic Type steps behind a **closed** `TypeRole` enum, so a screen cannot invent an eighth step and **no fixed point size is expressible anywhere in the token file**. That last property is why the ramp assertion could not be broken by mutation: the guarantee is carried by the type system rather than by a test. **Space Grotesk and Crimson Pro are therefore not on iOS**, and this is the open half: either the files get bundled with a Dynamic Type mapping, or the system families are blessed as the iOS answer and `DESIGN.md` says so. Ship-blocking for neither — the system families respect Dynamic Type correctly today — but it is a visible brand difference between desktop and phone. | **Closed, with a typography decision owed before release** |

| 120 | 2026-09-13 | `DESIGN.md` §Color | **`DESIGN.md` names colour roles it gives no values for, and one unnamed role turned out to be the binding accessibility constraint.** Hex is given for canvas, primary ink and secondary surface; `--text-primary/secondary/tertiary/bright`, `--surface-active`, the destructive colour and the hairline border have **names and no values**, while the same section requires small informative text to hit 4.5:1 "in every theme and interaction state" and rejects "tiny low-contrast metadata added for visual quietness". Reading `apps/desktop/.../base.css` to recover them would be reading an implementation for a specification, which is the defect this log exists to prevent. **`--surface-active` is the sharp edge**: it is the pressed and selected fill, it is the hardest background in the system, and a first set of tertiary-text picks failed AA against it at 4.25 and 4.01 while passing against both other surfaces. | The T160 subagent, which reported each gap with the value it chose and the reason, rather than quietly picking. | Closed by recording the iOS resolutions as **derived, not invented**: each was obtained by solving for the quietest value that still clears 4.5:1 against **all three** surfaces in each mode — canvas, secondary surface and `--surface-active` — so they are the output of `DESIGN.md`'s own stated constraint rather than a taste call, which is what makes them safe to ratify. The hairline border is the deliberate exception at 1.28:1 and 1.38:1: `DESIGN.md` asks for a quiet one-pixel separator and the grouping is also carried by spacing, so SC 1.4.11 does not apply to it. **That last reading is the implementer's and should be ratified explicitly** rather than inherited. Also recorded: iOS ships only the default theme pair, because the phone has no settings surface to choose among `DESIGN.md`'s four stored desktop themes, so light paper is unreachable and absent. | **Closed** |

| 121 | 2026-09-13 | `data-model.md` §C.1, 02 §2.9 | **A session cannot be restored on cold launch, and no chapter draws the edge that would allow it.** `AuthSession::new` sets `state: Mutex::new(AuthState::SignedOut)` unconditionally, while the keychain still holds a valid refresh token written by the previous run. §C.1's nine states and chapter 02's edges contain **no** transition from "the process started and there are tokens on disk" to `Registered` — the only ways into `Registered` are an OTP or a device registration, both of which require the user to do it all over again. So a registered user who quits the app and reopens it is shown the sign-in screen with a working session sitting in their keychain. **It would have survived the entire suite**: evidence gathered in one continuous session cannot see it, which is exactly what T161's "on a phone holding no prior state" exists to catch. | Reported by the T163 subagent; the orchestrator confirmed it directly at `crates/memry-core/src/api/auth.rs:274` rather than taking it on report. | Closed by T165 as **`AuthSession::restore()`** — an explicit, synchronous, retryable method — which is the shape this entry leaned toward, but **the reason recorded here first was wrong and is corrected**. The original argument was "a constructor that reads the keychain and may refresh can fail for network reasons, and `new` is infallible". Neither half survives the code: `new` already returns `Result<Self, ApiError>`, and the restore makes **no request at all**. The argument that does survive is the **keychain**. The five entries are `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` (data-model §B, R11), so a process starting after a reboot but before the phone's first unlock reads **`Locked`, not absent**. A probe inside `new` must either propagate that — and the app cannot construct a session or render at all until the phone is unlocked — or swallow it into `SignedOut`, **which is this very defect one layer down**. A method is retryable: the shell calls it at launch and again on protected-data-available, without rebuilding the session and without a second `TokenManager` over the same keychain entries, which chapter 02 §2.9 turns into a device revocation. It makes no request deliberately — an async core call cannot be cancelled (108) and the ladder honours `Retry-After` with no ceiling (109), so a restore that refreshed would be a **launch that can suspend for an hour and cannot be interrupted**; the `401`-and-refresh on the first `Auth::Session` call is the path `HttpClient` already owns. The three outcomes stay distinct by **shape, not spelling**: no refresh token → `Ok(SignedOut)`; a refresh token → `Ok(Registered)`, a _claim_ the server adjudicates; a locked store → `Err(SecureStore)` with the state left untouched so the retry is a retry. Landing in `Registered` is load-bearing: `SessionExpired` and `Revoked` are both edges **out of** `Registered`, so restoring anywhere else is what makes them unreachable. One event appended last, `SessionRestored`, so no existing discriminant moves. | **Closed** |

| 122 | 2026-09-13 | `contracts/core-api.md` | **Moving an exported type between Rust modules silently rewrites the FFI checksums of every method that mentions it**, and nothing in the contract said so. UniFFI folds a type's module path into its metadata checksum, so lifting `AuthState` into a new `api/auth/machine.rs` — a pure refactor that altered no signature — rewrote the checksums of all **eight** `AuthSession` methods returning it (`mark_revoked` 55264 → 40115, and so on) and **deleted nine lines** from the generated Swift. On a project where "no gate catches a stale binding" is already a standing hazard, a refactor that churns the ABI while looking like tidying is exactly the change that ships broken. | The T163 subagent, which performed the move, read the regenerated diff, understood why it was not additive, and **reverted it** to keep the binding diff purely additive — then reported the mechanism rather than only the outcome. | Closed by writing the rule into `core-api.md`: **an exported type's Rust module path is part of its ABI.** Moving one is a breaking change to every method that mentions it, even when no signature changes, so it is done deliberately and never as part of a refactor. The practical check is the one that caught it: after any change to the exported surface, regenerate and confirm the diff against the previous generated Swift has **zero deleted lines**. A purely additive change deletes nothing; anything deleted means an existing binding moved, and that is the moment to stop. | **Closed** |

| 123 | 2026-09-13 | `data-model.md` §C.1, 02 §2.13 | **`AwaitingProviderToken` had no exported edge out for a cancelled sheet, so the Google button could not be wired without stranding the user.** §C.1 draws a single edge for "cancelled, expired, or rejected", and `complete_provider_sign_in` applies it for **rejected only**. Cancellation is the shell's fact — the user closed the consent sheet and the core never hears about it — and `sign_out` from `AwaitingProviderToken` is `InvalidState`. So `begin_provider_sign_in` followed by a dismissed sheet left the session permanently in a state with no way out, and the symptom was visible from the UI side too: `SignInStep`'s `awaitingProviderToken` branch offered `primary: nil, secondary: nil`, a screen with no action at all. **One drawn edge covering three causes hid the fact that only one cause had an implementation.** | The T165 subagent, which found it while wiring T148's button and reported that the button could not be wired honestly without it. | Closed in code by **`AuthSession::abandon_provider_sign_in()`** — synchronous, no new state, no new event; it applies the already-drawn `SignInFailed` edge and clears the spent nonce. Deliberately _not_ a new state or a new event: the edge existed, only its call site did not, which is the same family as defects 103, 105 and 114. §C.1 and chapter 02 §2.13 must now say **which method draws which third** of that edge, because "cancelled, expired, or rejected" reading as one transition is precisely what let two thirds of it go unimplemented. T147's assertion that `SignInStep(.awaitingProviderToken)` offers no action was correctly falsified by this and was **updated rather than deleted**, with the reason written in. | **Closed** |

| 124 | 2026-09-13 | 13 §13.7.10, `contracts/core-api.md` | **A folder can hold notes and have no `folder_config` record, and no chapter says whether a folder tree must show it.** Chapter 13 §13.7.10 and `tests/domain_folders.rs` both make it legal for notes to live under a folder path that has no `folder_config` row — the path is carried on the note, and the config record is what gives a folder an icon, a name and an ordering. So the `folder_config` projection is **not** the set of folders that exist; it is the set of folders someone has configured. **T156 is a folder tree and will hit this on the first real vault**, where a folder created on another device, or created before a config record was written, simply will not appear while its notes do. | The T164 subagent, which exported `Notes::folders()` as the projection **honestly** and documented the hole rather than inventing a policy inside the core — which was the right call, because "should an unconfigured folder appear in the tree" is a product question and the core is the wrong place to answer it silently. | **NOT CLOSED — needs a decision before T156.** Three shapes, and they are genuinely different products: (a) the tree shows only configured folders, and a folder appears when someone configures it — simple, and loses folders that demonstrably hold content; (b) the tree is the union of `folder_config` rows and the distinct `folder_path` values on live notes, with unconfigured folders rendered from their path alone — truthful, and means the tree is a query rather than a table; (c) the core writes a `folder_config` on first sight, which is a **write** during a read and is refused here on those grounds alone. The shell must not paper over it by treating an absent folder as an empty one — that is the "unparseable reads as empty" rule in another costume. Recorded against chapter 13 because the chapter is where the answer belongs once it exists. | **Kaan decided, 2026-09-13: show only configured folders.** Option (a). The folder tree is the `folder_config` projection, exactly as T164 exported it, and a folder appears when it has a config record. Recorded with its cost stated rather than glossed: **a folder created on another device, or holding notes but never configured, will not appear in the iOS tree while its notes still appear in the note list.** That is a real and visible inconsistency, and it is accepted deliberately — the alternative makes the tree a query over two sources rather than a table, which is a larger change than a read-only companion needs. **The shell MUST NOT paper over it** by treating an absent folder as an empty one, or by hiding notes whose `folder_path` has no row: the notes are real and stay visible. If the inconsistency proves confusing on a real vault, option (b) — the union of config rows and distinct live `folder_path` values — is the upgrade, and it is additive. | **Closed** |

| 125 | 2026-09-13 | 12 §12.1, tasks.md T157, `contracts/core-api.md` | **T157 cannot render a note from anything the core exports, and chapter 12 does not say which of two different things "read a note" means on a shell.** §12.1 makes `extract_text` the only text operation a non-editor client owns, so T164's `NoteBody.text` is plain text — headings and list markers flattened, no markdown fidelity, no formatting. That is a legitimate read-only _preview_, and it is what a search result or a list subtitle wants. It is **not** what a note view wants: a shell that renders a note needs the encoded Y.Doc state to feed the editor bundle. `Document::encode_state()` exists in the core and is **not on the FFI surface**. Chapter 12 never distinguishes the two, so T157's task text — "read-only rendering happens in the shell" — reads as settled while being ambiguous between a preview it can build today and a render it cannot. | The T164 subagent, which exported the text form deliberately, said plainly what T157 would still be missing, and recommended re-scoping rather than quietly shipping the weaker thing under the stronger name. | **NOT CLOSED — T157 must be re-scoped or given the export before it starts.** The decision is whether US3's "browse real notes read-only" means a **text preview** (shippable now, no new export, genuinely read-only, and arguably the right first version of a companion app) or a **faithful render** (needs `encode_state` exported plus the editor bundle hosted on iOS, which is `EditorHost` — a seam that, per defect 107, **nothing in the core can be handed today**). Those are different sizes of task. Chapter 12 should gain a section naming the two reads and which export serves each, because the ambiguity is what let a task be written that cannot be done. Note the shape this shares with 114: a task written against a capability nobody checked existed. | **Kaan decided, 2026-09-13: preview, not render.** T157 ships the `extract_text` form T164 already exports — flattened plain text, no markdown fidelity — and **no new export is needed**. This is the right first version of a read-only companion and it is what US3's "browse recent content" actually asks for; a faithful render needs `Document::encode_state()` on the FFI surface **and** the editor bundle hosted through `EditorHost`, which per defect 107 nothing in the core can be handed at all, so it would have pulled band B3's write half and a seam redesign into a read-only phase. **T157's task text must say `preview` rather than `rendering`**, because "read-only rendering happens in the shell" is what made this ambiguous, and chapter 12 should name the two reads so the next reader does not have to rediscover that they are different. The cost, stated: a note with headings, bold, links or code blocks appears as plain text on iOS. | **Closed** |

| 126 | 2026-09-13 | 03 §3.9, §3.4 | **Chapter 03 states a retry policy for `POST /auth/linking/complete` that is correct only for a client whose poll loop is internal, and never says so.** §3.9 gives `{ maxRetries: 3, baseDelayMs: 2000, retryOn429: false }` and justifies it with "because the poll cadence is itself the retry" — but those are two different mechanisms, and the citation is the desktop, where they coincide. A client that polls from an **external** timer, which is what any shell with a UI does, inherits a contradiction: an inner ladder turns one poll into up to four requests **invisibly**, so §3.4's cap of 30 requests per 60 seconds per session is enforced by the server against a number the client never knew it sent; and on a platform where an in-flight call cannot be cancelled — iOS, where the generated bindings contain **no** `rust_future_cancel` (defect 108) — the ladder suspends the caller for up to 14 s inside one uncancellable call, during which a user cannot abandon a link they have changed their mind about. | The T235 subagent, which deviated from the literal clause **deliberately**, implemented `RetryPolicy::never()` on `complete`, and reported the deviation and its reasoning rather than quietly following or quietly diverging. That is the correct handling of a clause that does not fit: do the right thing and escalate the text. | Closed by amending §3.9 rather than by changing the code, because the code was right. The chapter now says a client with an external poll timer MUST use no ladder on `complete` and issue **exactly one request per poll**, with both reasons written out — budget accountability and abandonability — and states plainly that **the retry is the next poll**. `scan` is explicitly excluded: it is a single request, not a poll, and keeps the default ladder. The general lesson, which is why this is worth an entry rather than a code comment: **a policy quoted from one implementation carries that implementation's shape**, and §3.9 cited the desktop's line number without noticing that the desktop's ladder and its cadence are the same loop. | **Closed** |

| 127 | 2026-09-13 | `data-model.md` §C.2, tasks.md T155, `contracts/core-api.md` | **§C.2's vault-lock state machine has no export, so T155's task text describes something that cannot be built.** The task line says the picker drives "the core's `Unlocked -> VaultChoice` transition rather than computing one" — but `VaultChoice` returns **zero** hits in the generated Swift, as does any vault lock state. `core-api.md`'s band-B3 table still assigns "vault selection" to `Vault`, and T164's landed read slice has no selection surface: `Vault.open` opens a named vault and that is all. So the shell has no transition to drive and must hold the phase itself. | The T155 subagent, which held the phase in the shell, wrote the reason into the file header, and reported it rather than inventing a core call. | Closed **by amending the task text rather than by adding an export.** The shell owning a screen phase is not a defect on its own — what was wrong was a task line promising a core transition that does not exist, which is the fourth instance this phase of work specified against an unchecked capability (see 114, 124, 125). The picker is correct as built and the checkpoint does not need the machine: US3 asks the user to pick a vault and browse it, and a shell-owned phase does that. **If §C.2 is ever exported, the shell phase becomes the renderer of it and this screen's logic is deleted, not adapted** — recorded so the next reader does not preserve a shell state machine that has become redundant. | **Closed** |

| 128 | 2026-09-13 | 01 §1.4.2, `data-model.md` §C.2, `contracts/core-api.md` | **The local vault key verifier cannot be stored on iOS, so "this database belongs to a different master key" is undetectable there.** data-model §C.2 says "there are two verifiers and both are checked", and the unlock screen explicitly hands the second to T155. The functions exist and are exported — `derive_vault_key` and `local_vault_key_verifier(vault_key, vault_id)` — but there is **nowhere to persist the result**: chapter 01 §1.4.2 keeps it in the desktop's local settings table as `vault.crypto.verifier.v1`, the core exports no settings read or write, and `SecureStoreKey` is a **closed five-entry enum** (data-model §B). So the value can be computed on every launch and compared against nothing. The account-level verifier (§1.4.1) is checked and does work, so a wrong recovery phrase is still caught; what is not caught is a vault database that was written under a different master key — which is what §1.4.2 exists for. | The T155 subagent, which computed the reachable half, found no store for the other, and **did not invent a sixth keychain entry or a shell-side settings file** — either would have been a data-model change smuggled in under a UI task. | **NOT CLOSED.** It is a real hole in a stated invariant, and the honest options are a sixth `SecureStoreKey` entry (a data-model §B change, additive, needs a migration story for existing installs) or a settings export from the core (larger, and it is band B3's remainder). **It does not block the checkpoint**: the account verifier still catches a wrong phrase, and a mismatched vault database is only reachable by restoring a backup across accounts or by a bug in vault selection. Recorded rather than closed because "both are checked" is currently false on one platform, and a statement in the data model that is false in the shipping client is exactly what this log exists to surface. | **Open — not checkpoint-blocking** |

| 129 | 2026-09-14 | research.md R13, 03 §3.8 | **R13 says the QR carries "a short one-time code, not a key blob". Chapter 03 §3.8 says it carries a key blob.** §3.8 is normative and explicit: the payload is `JSON.stringify({ sessionId, ephemeralPublicKey, linkingSecret, expiresAt })` — about **200 characters**, including a 44-character base64 `linkingSecret` and a base64 X25519 point. R13's sentence is what the "manual code entry" fallback was designed around, and **that fallback is mandatory**, because the camera does not exist in CI or on the simulator. So the one requirement that had to work without a camera was specified against a payload shape that does not exist: nobody types 200 characters of JSON. | The T153/T154 subagent, which read both, implemented against the normative one, and reported the contradiction instead of quietly shipping a text field nobody could use. | Closed by **correcting R13 in place** rather than by changing the screen. iOS implements the fallback as a first-class **paste** field — always visible, not behind a disclosure, with its own label and button, handing the pasted bytes to the core **verbatim** (asserted byte-for-byte including surrounding whitespace, because the core is the only parser). That is a real fallback: all four wiring tests drive the entire production chain through it with no camera at all. A genuinely typeable short code would need a desktop change **and** a server change, both outside this feature. The general shape is worth noting: **a research entry and a protocol chapter disagreed, and the research entry was the one the task text had been written from.** | **Closed** |

| 130 | 2026-09-14 | `contracts/shell-seams.md`, research.md R13 | **The `CodeCapture` seam exposes no camera preview, so scanning is blind.** Its whole surface is `permission()`, `requestPermission()` and `scan() async throws -> String`. There is no `AVCaptureVideoPreviewLayer`, no preview view and no frame callback, so the scan screen can tell the user the camera is running but **cannot show them what it is pointed at**. Aiming a phone at a screen without a viewfinder is a guessing game, and the failure is silent — the scan simply never resolves. | The T153/T154 subagent, while building `QRLinkView` against the landed seam. | **NOT CLOSED.** It is a genuine usability hole and it is **not** fixable inside the seam as designed: a preview is a `CALayer` that must reach SwiftUI, which is a different shape from a request-response method and touches `shell-seams.md`'s closed eight-seam list. **It does not block the checkpoint** — the paste fallback is a complete path and T161 can link every vault through it — so it is recorded rather than rushed. The options are a preview-returning addition to `CodeCapture`, or a shell-only viewfinder that runs alongside the seam without crossing it (cheaper, and keeps the seam list closed, but duplicates the capture session). **Decide before the camera path is put in front of a real user**, not before T161. | **Open — usability, not checkpoint-blocking** |

**Open**: 2 — **128** (the local vault key verifier has nowhere to live on iOS) and **130** (no camera preview; usability). Neither blocks the checkpoint. **Closed**: 128. **Logged**: 130.
