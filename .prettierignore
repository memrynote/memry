node_modules
out
dist
build
coverage
pnpm-lock.yaml
package-lock.json
yarn.lock
LICENSE.md
tsconfig.json
tsconfig.*.json
.env*

# Generated by codegen scripts (rpc:check / ipc:check do exact-string compares;
# Prettier-formatted output won't match the generator's raw output).
apps/desktop/src/main/ipc/generated-ipc-invoke-map.ts
apps/desktop/src/preload/generated-rpc.ts
# Same rule: `editor:check` compares the stamped hash, and the payload is a
# 1 MB base64 literal that Prettier reflows into something the check cannot read.
apps/mobile/src/editor/generated/

# Golden byte-preservation fixtures: bytes (CRLF, BOM, missing final newline)
# ARE the test contract — formatting them destroys what they assert.
apps/desktop/src/main/vault/__fixtures__/golden-vault/
