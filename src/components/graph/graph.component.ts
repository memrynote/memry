import { GraphNode } from '../models/graph-node'; import { CommentService } from '../services/comment.service'; @Component({ selector: 'app-graph', template: '<div> <app-graph-node *ngFor="let node of nodes" [node]="node"></app-graph-node> </div>' }) export class GraphComponent { nodes: GraphNode[]; constructor(private commentService: CommentService) { } async ngOnInit(): Promise<void> { this.nodes = await this.commentService.getCommentLinkedNodes('note-id'); } }