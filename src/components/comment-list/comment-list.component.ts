import { Comment } from '../models/comment'; import { CommentService } from '../services/comment.service'; @Component({ selector: 'app-comment-list', template: '<ul> <li *ngFor="let comment of comments"> {{ comment.text }} </li> </ul>' }) export class CommentListComponent { comments: Comment[]; constructor(private commentService: CommentService) { } async ngOnInit(): Promise<void> { this.comments = await this.commentService.summarizeUnresolvedComments('note-id'); } }