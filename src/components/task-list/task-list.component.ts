import { Task } from '../models/task'; import { CommentService } from '../services/comment.service'; @Component({ selector: 'app-task-list', template: '<ul> <li *ngFor="let task of tasks"> {{ task.text }} </li> </ul>' }) export class TaskListComponent { tasks: Task[]; constructor(private commentService: CommentService) { } async ngOnInit(): Promise<void> { this.tasks = await this.commentService.convertCommentToTask('comment-id'); } }